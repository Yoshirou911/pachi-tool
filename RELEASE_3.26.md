# v3.26.0 店舗×イベントの予測比較

完了日：2026-09-10。改訂予測特化工程の3.26のみ。3.27には進んでいない。

## 実装

- 店舗名を統一し、店舗×同名イベント単位で比較する研究用モデルを追加。別名・別店舗・同日の複数イベントを混ぜない。媒体ランク末尾の表記だけ正規化する。
- イベントの取得時刻を確認し、予定も実績も対象日の2日前までで切る。前日13時より後の告知を混ぜないため、日単位処理では前日全体を除外する保守的な仕様。
- 同じ曜日・前56日内で、当時確認できるイベント登録のない日を比較対象とする。確認済み通常営業日とは限らない。
- 比較日2日以上、比較可能な同名イベント3日以上で研究用候補を計算。イベント差を `n/(n+8)` で縮小し±300枚に制限する固定モデル。補正なしの同曜日平均と同じ評価日に差枚の平均絶対誤差を比較する。
- 過去の各日を順番に、その時点で使える予定・過去日実績だけで試算。比較5日未満は検証不足。5日は精度保証・本番採用の条件ではない。
- 新モデルは未採用。3.30の採用審査まで自動加点しない。既存の狙い台検索にあった、対象イベント群をひとまとめにする旧補正も停止した。基本の店舗・機種・台番号予測は継続。
- みんレポの単なる記録日・予測対象外の情報はイベント比較一覧にも含めない。取得時刻不明は参考表示に留める。
- スマホ・PC／Windows共通画面に、店舗→イベント→比較結果・不足理由・出典・日別答え合わせの開閉欄を追加。
- 従来イベント画面は「規則から補完した日を含む従来集計／予定の取得時点は未監査」と明記。従来の率を3.26の実績と混同しない。
- 既存の前日固定処理を実行すると、新しい比較結果と元イベント情報も同じ固定ペイロードに保存する。旧予測・旧比較レポート・元実績は削除も上書きもしない。
- 今後の取得・手動追加の予定、取得イベント結果にUTCオフセット付きの時刻を保存。従来の時差なし時刻はJST PCとUTCサーバーのうち遅い側（UTC解釈）で保守的に扱う。
- 全クライアントの版番号・更新履歴・キャッシュ版を3.26.0へ更新。

## 検証

- Python全304テスト成功。既存のStarlette/httpx非推奨警告1件。
- JavaScript共通検証UI・スマホUI・PC UIの契約テストと構文チェック成功。
- 新規テスト：取得時刻不明、後日取得、前日夜の告知、別店舗・別イベント、重複と矛盾、欠損、重複イベント日、比較日不足、通常の実績記事の除外、時差、固定ペイロードの不変性。
- スマホ幅390pxで実データの店舗・イベント詳細、未計測・不足理由・出典を確認。PC版でも共通パネルの開閉を確認。
- 自動収集・前日固定を起動しない検証サーバーを使用。実データの新しい予測を手動固定する操作は行っていない。

## 実データの測定結果

条件：四條畷駅周辺、対象日2026-09-10、直近120日、70%モード。3.26の予定・実績期限は2026-09-08。

| 店舗 | 比較欄のイベント種類 | 比較可能な過去イベント日（種類別合計） | 時点制限付き誤差比較日 |
|---|---:|---:|---:|
| ひま・わり四條畷店 | 0 | 0 | 0 |
| キコーナ四條畷店 | 5 | 0 | 0 |
| キコーナ大東店 | 4 | 0 | 0 |
| キコーナ野崎店 | 3 | 0 | 0 |
| スーパーコスモプレミアム大東店 | 0 | 0 | 0 |
| ニコニコ住道店 | 8 | 7 | 0 |
| ベガスベガス大東店 | 8 | 6 | 0 |
| マルハン大東店 | 1 | 0 | 0 |

計29種類。比較可能な過去日があることと、その日より前から予定を把握し予測できたことは別。3.26の実測の誤差改善・的中率はまだ未計測であり、従来の成功率を流用しない。

## 限界と次工程

- 公開結果の取得時刻・改訂履歴の完全な保存は未整備。この検証は研究用で、本番事前成績ではない。
- 取得済みスマスロ集計の機種構成・稼働・公開日の偏りが残る。イベントの因果効果、全店平均、高設定、本人収支を表すものではない。
- 告知を前々日までに制限するため、前日・当日告知は現版の研究用候補に使わない。より細かな時刻単位処理は今後の課題。
- モデル採用、校正、精度向上の保証はまだ行わない。3.27は店舗×機種、3.30はモデル比較・採用審査。

## 配信

ローカル実装のみ。GitHub push、公開API／GitHub Pagesの配信、EXE再ビルドは未実施。既存の公開スマホ版・EXEが更新された意味ではない。
