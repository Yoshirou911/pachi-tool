# v3.43.0：複数AIの共通条件比較

実装日：2026-09-19。ローカル実装。次は3.44「AI回答の自動評価」。

## 追加したもの

- `api/ai_comparison.py`：Groq・DeepSeek・Qwen・Kimi・Claudeへ、同じ店舗・対象日・期間、同じ固定問題、同じ3.42根拠スナップショットを渡す比較ハーネス。
- GET `/api/ai/comparison/status`：APIキーを返さず、各社のモデル、設定有無、比較実測の準備、単価設定有無、固定問題を表示。
- POST `/api/ai/comparison/run`：初期値は`preview`。準備確認ではクライアントも作成せず外部通信0回。`external`はリクエストの明示確認、`PACHI_AI_ALLOW_EXTERNAL=true`、`PACHI_AI_ALLOW_COMPARISON_EXTERNAL=true`の全条件が必要。
- `mobile/ai-comparison.mjs`：PC・スマホの「店舗×強い機種」の下に共通比較画面を追加。AIと固定問題を選択し、外部通信なしの準備確認と料金発生の可能性がある実測を分離。
- 通常回答と比較で`evidence_messages`を共用。AIの生文章・値・新判定は受け取らず、3.41の根拠ID契約だけを検証する。
- 接続先の応答にトークン使用量がある場合だけ記録。管理者が会社別の入出力単価を設定した場合だけ費用を計算し、欠損は推測せず未計測にする。

## 比較値の意味

- 契約適合率：根拠IDだけを返す固定形式に合格した問題の割合。
- 固定期待根拠との一致：質問別にサーバーが抽出した先頭8件以内の根拠と、AIが選んだ根拠のJaccard一致率。一般的な回答品質・予測的中率ではない。
- 応答時間：各API呼び出しの経過時間。通信状況を含み、モデル能力だけの値ではない。
- 費用：接続先が報告した入出力トークン数と管理者設定単価が両方ある場合だけ算出。公式価格の自動取得や推定単価は使わない。
- 比較順は入力順を維持し、勝者・既定AI・実戦AIを自動決定しない。予測順位、着席判定、勝率へ反映しない。

## 安全条件

- 固定問題は機種、曜日、日付末尾、台番号履歴。1回につき最大3問、最大5社。画面に最大呼出回数を事前表示する。
- ブラウザから自由な質問文、個人収支、会話履歴、実戦履歴を比較APIへ送らない。APIキーと生回答もDB・ブラウザへ保存しない。
- 未設定の会社は呼ばない。1社の失敗・形式不正は区分だけ記録し、秘密や生のエラーを返さず他社の比較を継続する。
- 店舗・日付・期間が実行中に変わった結果は画面へ表示しない。

## 検証

- Python全体508件成功。比較の二重許可、準備確認0通信、同一スナップショット、未設定会社の非呼出し、自由文拒否、トークン・単価、API入力上限を確認。
- JavaScript画面テスト全スクリプト成功。比較画面の入力、HTML無害化、版参照と既存画面契約を確認。PC・スマホ・Service Workerの構文チェック成功。
- 実DBのベガスベガス大東店、対象2026-09-20、90日で準備確認。機種30件・曜日7件の根拠を2社へ共通化する条件を作成し、外部呼出し0回、実戦判定変更なしを確認。
- 実ブラウザの手動操作と外部AI実通信・実料金は未試験。Starlette TestClientの既知の移行警告1件あり。

## 配信状態

GitHub push、公開API／PWA配信、EXE再ビルドは未実施。外部AIの実通信・課金も行っていない。ローカル起動を再読み込みすると3.43の準備確認を使える。

3.44の自動評価、AIの独自学習、実戦採用、予測精度・利益の改善測定には進んでいない。
