# v3.45.0：画像を端末内で読み取り、確認して保存

実装日：2026-09-19。ローカル実装・自動検証済み。次の工程は3.46「誤回答防止」。

## 使い方

PC・スマホとも「狙い台捜索 → 店内 → 画像を読み取る」を開く。データランプ、店舗資料、ホールマップのいずれかを選び、画像を撮影または選択して「端末内で読み取る」を押す。候補値を元画像と見比べて訂正し、「元画像と入力値を自分で照合した」にチェックして保存する。

- データランプ：現在ゲーム数、台番号、BB、RBを確認。保存後、現在ゲーム数と台番号をハイエナ判定欄へ反映する。
- 店舗資料：読み取った文字を確認・訂正して記録する。資料の文面を命令や確定情報として自動実行しない。
- ホールマップ：確認できた台番号だけを保存し、店内マップ編集欄へ未確認の仮配置として反映する。島、列、隣接、座標、機種は自動確定しない。

## 実装

- `mobile/image-analysis.mjs`をPCとスマホで共用。ブラウザの`TextDetector`があれば文字候補を抽出し、なければ既存7セグ数字認識を試す。自動抽出できない場合も画像を見ながら手入力できる。
- 対応画像はJPEG・PNG・WebP、20MB以下。プレビューは縮小再描画し、EXIFなどの付随情報を抽出記録へ入れない。
- 保存前に人の照合チェックが必須。種類ごとの必須値、整数範囲、台番号重複、候補数、文字数をブラウザとAPIの両方で検証する。
- `api/routers/image_analysis.py`の入力契約は未知フィールドを拒否し、画像base64・ファイル名・生バイトを受け付けない。GET `/api/image-analysis/status`、GET/POST `/api/image-analysis/records`を追加。
- `api/image_analysis.py`は`DATA_DIR/image_analysis.db`へ画像SHA-256、形式、寸法、サイズ、抽出候補、確認値だけを追記保存する。更新・削除トリガーで過去記録の上書きを拒否する。
- 元画像は既定で保存しない。「元画像もこの端末だけに保存」を選んだ場合だけブラウザのIndexedDBへ抽出記録IDと分離して保存する。サーバーや別端末へ同期しない。
- ホールマップ画像は`source_kind=uploaded`、`verification_status=未確認`で編集画面へ反映する。保存ボタンを押すまではレイアウトDBにも入らない。
- 画像対応AIは接続先ごとに形式・対応モデル・費用・個人情報条件が異なるため、この版では無効。既存AI許可やAPIキーがあっても画像を外部送信しない。

## 確認結果

- Python全体539件成功。新規9件で読取り専用状態確認、3種類の保存、追記専用DB、元画像非保存、未知画像データ拒否、未照合拒否、種類別必須値、台番号重複、端末保存希望の表現を検証。
- UIテスト全スクリプト成功。新規テストで台番号抽出、値検証、照合必須、プレビュー非送信、HTML無害化、画面表示だけではPOSTしないこと、PC・スマホ共通配置、Service Worker登録を確認。
- `mobile/image-analysis.mjs`、`mobile/app.js`、`web/js/app.js`の構文確認成功。
- Starlette TestClientの既知のhttpx移行警告1件。実ブラウザ、実iPhoneカメラ、Windows EXEでの手動確認は未実施。

## 限界・配信状態

`TextDetector`非対応ブラウザでは、店舗資料とホールマップの汎用文字OCRは自動化できず、7セグ数字または手入力になる。写真の傾き、反射、解像度、表示字体によって誤読するため、照合チェックは精度保証ではない。画像から高設定、イベントの強さ、実際の島・隣接、期待値を推測しない。端末内元画像はブラウザデータ削除で消え、バックアップ・同期対象外。

GitHub push、公開API／スマホPWA配信、EXE再ビルドは未実施。ローカルサーバーを再起動して画面を読み込み直すと3.45を利用できる。今回、外部画像AIへの送信・課金・APIキー設定はしていない。
