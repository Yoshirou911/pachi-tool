# v3.44.0：AI回答の固定評価・履歴保存

実装日：2026-09-19。ローカル実装・検証済み。次の工程は3.45「画像分析」。

## 使い方

「狙い台捜索 → 店舗×強い機種 → AI回答の自動評価」。方式と各問の反復回数（1〜3回）を選び「評価を開始して保存」を押す。進捗、問題別の正解・回答、不足した根拠、保存履歴を確認できる。固定問題なので上部の店舗選択には依存しない。

初期値の「ローカル参考方式」は外部AIを使わず、先頭8件までの根拠を返す単純方式を採点する。AI各社の成績として表示しない。外部AI評価は3.43のサーバー側送信許可2種類・会社別キー・画面の実行確認が必要。1回につき1社、最大11問×3回＝33回。自動的な有料再試行はしない。

## 実装

- `data/ai_eval/suite_v1.json`：手作業の正解根拠・禁止根拠・判断不能を持つ11問。店舗、機種、曜日、少数イベント、入替、確定設定、配置、未来結果、出典、資料内の指示、空データを扱う。**架空の検証用データ**であり、実店舗の正解設定や予測精度の測定ではない。
- `api/ai_evaluation.py`：問題集を読み込み、入力期限より後の結果・後日改訂を除外。正解・採点理由・後日結果を外部AIへのプロンプトに含めない。3.41の形式照合を共用し、評価時だけ空配列の棄権を許可。既存の通常回答の採用基準は変更しない。
- 正解根拠の完全一致率、形式適合率、選択根拠の適合率、必要根拠の回収率、禁止／不要根拠、棄権成功率を分離。反復一致は有効回答の比較可能な組数も表示し、同じ誤答が続く場合を正解と混同しない。
- `data/ai_evaluations.db`：開始前に問題・入力・正解・採点方式・版・モデル・単価を保存し、応答ごとに採点結果を追記。完了時の集計も固定保存。更新／削除を拒否する表を使い、後から問題を変えても旧成績を再採点しない。問題内容とプロンプトのハッシュで比較条件を識別する。
- HTTP開始要求はUUIDで重複を防止。同じID・同じ条件なら以前の結果を返し、異なる条件なら拒否。開始直後に応答が途切れても、同じIDで確認できる。サーバー再起動後の未完了記録は「中断」で保持し、自動的に課金処理を再開しない。
- 1サーバープロセス内の1ワーカーで評価し、進捗を取得。ローカルの既存1ワーカー運用向けで、複数プロセス間のジョブ管理は対象外。
- 接続エラー・形式不正は正解へ含めず、元の自由文・秘密・エラー詳細を保存しない。拒否した回答でも報告されたトークン使用量は保持。使用量・単価が欠ける総費用は未計測。実課金額・キャッシュ割引等の請求照合は未実装。
- GET `/api/ai/evaluation`、POST `/api/ai/evaluation/runs`、GET `/api/ai/evaluation/runs/{id}`。GETだけでDB作成や評価開始はしない。`mobile/ai-evaluation.mjs`をPC・スマホで共用。EXEのビルド定義にも問題集を追加。

## 確認結果

- Python全体530件成功。新規22件で未来情報除外、正解と不要根拠、棄権、自由文拒否、問題変更後の旧結果保全、追記専用DB、重複要求、同時実行、再起動、外部許可、拒否回答の費用、反復誤答、API入力を検証。
- UIテスト全スクリプト成功。新規の操作テストでは画面表示時の非実行、開始応答紛失時の同一ID再確認、実行中の進捗復帰、HTML無害化、PC・スマホの共通配置・キャッシュを確認。JavaScript構文チェック成功。
- ローカル参考方式を実際に11問×2回実行し、1件保存。形式適合100%、完全一致4/22回（18.2%）、不要根拠26件、棄権成功20%、反復一致100%（11/11組）、外部呼出0回。これは「同じ形式・同じ回答でも内容が適切とは限らない」ことを確認する参考値で、既存の店舗分析全体や外部AIの性能ではない。
- Starlette TestClientの既知の移行警告1件。実ブラウザでの手動操作、実iPhone・EXE起動、外部AI実通信は未試験。

## 限界・配信状態

評価基盤と検証問題の動作確認まで。実店舗の当時保存した入力・独立した正解ラベルを使う大規模評価、AI各社の実成績、自由な文章の正しさ、実戦予測の改善、独自学習・自動採用は未実施。後日の架空結果を分離するテストを、実店舗の先読みなし過去検証として扱わない。

GitHub push、公開API／スマホPWA配信、EXE再ビルドは未実施。ローカルサーバーを再起動して画面を読み込み直すと3.44を利用できる。今回、外部AIへの送信・課金・キー設定はしていない。
