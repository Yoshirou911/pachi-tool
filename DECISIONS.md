# 決定事項と検討中の案

更新：2026-09-21。方針変更時は理由と日付を残し、古い案を現在の決定として扱わない。

## これまでの方針・実装上の制約

- **予測特化**：一度に1バージョンを実装・検証し、次へは依頼後に進む。4.0までの順序は [工程表](PREDICTION_ROADMAP.md) を参照。10.0までの一括実装は合意していない。
- **地域・モード**：四條畷駅周辺を重点対象とし、ハイエナ・スマスロ狙い台・ジャグラーを混同しない。
- **根拠を優先**：欠損を0や負けにしない。未来の実績、後から知った配置、入替前の別期間を混ぜない。結果の公開時刻・改訂履歴の未整備は限界として残す。
- **研究と本番を分離**：新方式の採用審査は3.30。研究順位1位は着席許可ではない。過去検証、事前固定予測、高設定的中率、本人の勝率・収支は別の指標。
- **数字を作らない**：候補を出すためだけに基準を下げない。実測がない精度・完成度・利益を保証しない。番号の連続だけで角や隣接を断定しない。
- **保存を保護**：元実績、固定した予測・比較レポート、既存の編集を保持する。公開・GitHub push・EXE更新はそれぞれ別に確認して報告する。

## 3.49で具体化した扱い（2026-09-21）

- 自動テストの成功は実装の検証であり、各社AI品質・実戦予測精度の認定ではない。開発用評価・点検・管理は採用、説明・検索・画像は補助限定、実測していない外部AIは保留とする。
- AI10機能の認定は現在の関連ソース・資料・テストと検証記録の照合に従う。変更・欠落・不正記録なら保留に戻し、接続設定だけで未審査のAIを自動採用しない。
- 外部AIによる予測順位・着席判定の変更はこの版で不採用。次の3.50も統計判定をAIの文章で置き換えない。
- 料金は排他・一括予約、送信前の実行権の追記、受付時の単価固定で管理。未知の使用量は予約を保持し、同じ実行IDを再送しない。利用事業者の請求上限保証とは区別する。
- 画像の未確認OCR候補は送信・新規保存・再表示しない。旧DBの候補は勝手に削除しない。登録最新版が使えない場合に資料の旧版を復活させない。
- 3.49はローカルだけ。実機確認、有料実測、公開配信、EXE再ビルドは未実施。検証・限界は `RELEASE_3.49.md`。

## 3.29で具体化した扱い（2026-09-13）

- 確認済みの島・列・列内順から連続3台を作る。座標・番号順では推定しない。最新の未確認配置を古い確認済み配置で置き換えない。
- 差枚の翌日傾向と、本人の両日確定設定の比較を分ける。同値は据え置きの証拠とはしない。
- 記録時点がある配置だけで過去比較する。今日登録した配置を昔から知っていたことにしない。
- 3.29は試算・検証・表示まで。現行判定への新得点の採用は3.30へ持ち越す。
- PC・PWA共通実装と引き継ぎ資料を更新。GitHub push・公開配信・EXE再ビルドは行っていない。

## 3.30で具体化した扱い（2026-09-13）

- 研究試行は再利用するが、採用審査には新しい審査条件と実装識別を同時保存した事前予測だけを使う。旧予測の遡及的な書換えはしない。
- 直前の完了した日曜まで84日を固定し、同一対象の候補・基準・現行予測を比較する。必要日数・改善幅・不確実性の詳細は [RELEASE_3.30.md](RELEASE_3.30.md) を正本とする。
- 根拠と審査結果は追記保存。4系列とも現時点では保留で、「採用可」が出ても実戦適用済みとはしない。切替・復帰は根拠が揃ってから別途実装する。
- イベントの同一定義の現行予測不足は保留として残す。台番号の差枚誤差を順位的中率・本人の勝率へ読み替えない。

## 3.31で具体化した扱い（2026-09-13）

- 3.31（2026-09-13）：現行・3基準方式の確率／幅を事前記録で検証する。予測開始前に判明した結果だけで試験補正を作り、未計測の旧予測を後付けで補完しない。実戦への補正適用は行わず、同じ実装での蓄積を必要条件とする。詳細条件は [RELEASE_3.31.md](RELEASE_3.31.md)。

## 3.32で具体化した扱い（2026-09-13）

- 3.32（2026-09-13）：上位1・3・5件の比較用順位を前日固定する。現行判定通過の絞り込みを別にし、欠損時の下位差替えを禁止。旧予測への後付けルールは研究区分へ分離し、候補母集団全件を照合した共通日で比較する。自動的な最適K採用・実戦順位変更は行わない。詳細は [RELEASE_3.32.md](RELEASE_3.32.md)。

## 会議と開発の分け方

- 新しいチャットで会議・相談を行い、方針を決める構想がある。新チャット自体はまだ作成していない。
- 会議の依頼では実装を始めない。決まった内容を本ファイルへ反映し、実装事項は`TODO.md`へ移す。
- 毎バージョンのチャット新規作成は必須ではない。作業目的の変化や仕様混同が目立ったときに区切る。

## 消費を抑える作業方針

- 検索で対象を絞り、必要な関数・差分だけ読む。長いコード、ログ、画面全体を繰り返し出力しない。
- 関連テストを先に実行。予測・保存など影響の大きい変更では最後に全体テストも実行する。
- 成功した確認は、追加変更・失敗・未解決の懸念がない限り繰り返さない。軽微な文書変更で全体テストを回さない。
- テスト成功件数と失敗の詳細を必要量だけ表示する。品質監査・時点制限・データ保護の確認は削らない。
- モデルを下げた結果の手戻りも含めて節約を評価する。削減率は未測定。
- 詳細は最新リリース記録へ集約し、他の文書は要約と参照を更新する。`SOURCE_SNAPSHOT.txt`は毎版生成せず、外部引き継ぎの依頼時に生成する。

## 3.46で具体化した扱い（2026-09-19）

- 形式適合と根拠の適切さを分ける。通常回答・比較・評価で同じ決定的点検を使い、AIの棄権を一般統計で補完しない。旧版の完了評価は上書きしない。
- 画像の照合済み入力は確定設定・配置・イベント認定ではない。保存と他画面への反映を分離し、確認済みマップと別の保存キーを使用する。
- 規則による点検は資料の真偽や全自然文の解釈を保証しない。実戦精度向上と同一視しない。詳細は `RELEASE_3.46.md`。

## 3.47で具体化した扱い（2026-09-19）

- 専用知識は、同梱資料と登録設定を限定して検索・引用する層。外部AI通信、重みの追加学習、会話・OCR・個人履歴の自動採用はしない。
- 期待値は登録条件を明示選択して引用し、当日の着席可否を判定しない。確認日・適用期間・情報到達時点を混同しない。店舗参照先の登録を、営業・開催・出玉傾向の認定としない。
- 版・出典・内容ハッシュを付け、期限外・撤回・競合を除外する。新しい資料が使えない場合も古い版を自動復活させない。固定検索12問の一致は予測的中率ではない。詳細は `RELEASE_3.47.md`。

## 3.33で具体化した扱い（2026-09-14）

- 直近28日と以前56日を同店・同対象・同版・同実装・方式別に点検する。必要日数・照合率・共通対象割合・鮮度を先に固定。
- 共通対象全件が照合できる日だけで、予測誤差の拡大と公開実績の変化を分ける。直近の前半・後半の継続条件を採用。
- 警告は暫定的な見直し目安。データ不足・構成の変化を悪化と断定せず、モデル・順位・着席判定は変更しない。常時通知は追加しない。
- 事前記録の不足は旧版成績の合算や後付け予測で埋めない。詳細・限界は `RELEASE_3.33.md`。

## 3.40〜3.49のAI専用工程（2026-09-14決定）

- 3.40〜3.49を、接続基盤、根拠付き回答、店舗特化分析、複数AI比較、自動評価、画像分析、誤回答防止、専用知識、費用・個人情報管理、採用審査の順で実施する。
- OpenAI製モデルをアプリ内AIの既定候補として優先しない。Qwen・DeepSeek・Kimi・Claude等を同じ固定問題と評価条件で比較する。
- LLMは統計・機械学習による予測値を説明・検索・点検する層とし、未計測の確率や台番号を生成して本番判定を書き換えない。
- 「専用知識検索」と「モデルの独自学習」を区別する。まず根拠を検索して引用する仕組みを作り、正解付き事例が十分に蓄積するまで追加学習を前提にしない。
- 各版を一つずつ実装・検証する。3.49の採用審査を通過するまで、AI比較結果を実戦順位へ自動適用しない。

## 3.40で具体化した扱い（2026-09-14）

- 外部AIはサーバー環境変数で明示した1社だけへ接続する。自動的に別会社へ再送信せず、複数社比較は3.43まで行わない。
- APIキーは環境変数からだけ読み、状態API・DB・ブラウザ・ログへ値を出さない。個人の実戦履歴は明示許可がない限り送らない。
- AI未設定・停止・通信失敗・認証失敗・形式不正時は統計説明へ戻す。AI失敗を予測不成立や見送り判定へ読み替えない。
- Groqの廃止済みLlama指定は移行候補Qwenへ変更。他社モデルを含む品質比較や既定AIの決定は3.43・3.49へ持ち越す。
- 外部実通信、契約、課金、公開サーバーの秘密設定、GitHub push、PWA配信、EXE再ビルドは行わない。

## 3.41で具体化した扱い（2026-09-14）

- AIは根拠IDを選択し、文章・数字・判定はサーバーが固定表示する。自由文の照合を指示文だけに依存しない。
- 出典・期間・取得時刻がない場合は未記録とする。説明生成日時で元取得日時を置き換えない。
- 3.40の外部送信既定を訂正し、3.41は明示的な有効化まで停止。個人履歴・会話履歴・実戦推測値は外部送信しない。
- 一般チャットの過去集計は記述統計。現在配置・当時の情報到達時刻を保証せず、過去予測成績や勝率へ読み替えない。詳細は `RELEASE_3.41.md`。

## 3.42で具体化した扱い（2026-09-19）

- 店舗質問は固定語・選択項目で分類し、3.41の根拠ID照合・固定説明を維持する。AIの自由文で新しい勝率・設定・着席判定を作らない。
- 曜日・日付末尾は同店のその他の日、機種は同日の公開他機種と比較する。公開範囲の偏りと少数日を明示し、比較相手の出典も含める。他店を優良店／回収店と断定しない。
- 台番号の入替・空白期間を分離し、島は当時までに記録した確認済み配置だけ使用。番号順で隣接を補わず、新しい未確認配置を古い確認済み配置で代用しない。
- DBは読み取りのみ。外部通信は明示有効化まで停止。3.42は過去実績の説明範囲を拡張する版で、予測精度向上を実測した版ではない。詳細は `RELEASE_3.42.md`。

## 3.43で具体化した扱い（2026-09-19）

- 比較は会社名で順位を決めず、同じ固定問題・同じ凍結済み根拠を各社へ渡す。根拠契約の適合、一致、時間、トークン、費用を混ぜずに表示する。
- 初期値は外部通信なし。実測には通常の外部AI許可、比較専用許可、画面の料金確認をすべて要求する。未設定会社は呼ばず、個人履歴・自由質問・生回答を保存しない。
- 費用は接続先報告トークンと管理者設定単価が揃った場合だけ計算。欠損を推定しない。比較は実戦予測の精度検証ではなく、勝者・既定AI・着席判定を自動変更しない。詳細は `RELEASE_3.43.md`。

## 3.44で具体化した扱い（2026-09-19）

- 正解は問題集に手作業で固定し、先頭の根拠やAIの出力から作らない。正解・後日結果は入力へ送らず、入力期限後のデータと改訂を除外する。初期11問は架空検証資料と明示し、実店舗予測の成績とはしない。
- 棄権は評価時の空配列で採点。通信失敗・不正形式を正しい棄権としない。反復一致は有効回答の比較組数を明示する。
- 開始前の問題・条件、各回答の採点結果、完了時集計を追記専用DBへ保存。リトライは同一UUIDなら再呼出しせず、再起動で中断した有料処理も自動再開しない。
- ローカル参考方式は先頭8根拠を返す非AI方式。実AIの結果と混ぜず、実戦判定を変更しない。採点変更時はPROTOCOLを更新する。詳細は `RELEASE_3.44.md`。

## 開発担当モデルの検討案（最終決定・設定反映は未実施）

アプリ内部へ組み込むAIの選定とは別。下表は直近会議での提案であり、実測済みの最適設定ではない。

| 工程 | 開始時の候補 |
|---|---|
| 3.29 配置分析、3.30 モデル採用、3.31 確率補正 | GPT-6 Astra / high |
| 3.32 候補数と的中率の評価 | GPT-6 Astra / medium |
| 3.33 予測劣化の監視 | GPT-6 Astra / high |
| 3.40 AI接続基盤、3.43 複数AI比較、3.45 画像分析、3.48 費用・個人情報管理 | GPT-5.6 Sol / high |
| 3.41 根拠付き回答 | GPT-6 Astra / high |
| 3.42 店舗特化分析、3.44 自動評価、3.46 誤回答防止、3.47 専用知識 | GPT-6 Astra / xhigh |
| 3.49 AI工程の採用審査 | GPT-6 Astra / max |
| 3.50 朝一画面、3.70 現場UI | GPT-5.6 Sol / medium |
| 3.60 事前予測評価、3.80 配信・復旧、3.90 本番点検 | GPT-6 Astra / high |
| 4.0 点検通過後のリリース | GPT-6 Astra / medium |

- XHighやMaxを全作業の標準にはしない。確定仕様の表示・文言・版更新は低い思考量を使い、複合分析・誤回答防止・採用審査に限定する。
- モデル名・利用可否・設定は作業時に確認する。表の記載は自動切替や変更の許可ではない。
- AIを強くしても、同機種・複数台の履歴不足そのものは解消しない。
- プラグイン整理やモデル設定変更は未実施。不要候補の提案を、アンインストール済みと扱わない。
