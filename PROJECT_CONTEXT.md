# PACHI TOOL：引き継ぎの入口

更新：2026-09-21。新しい会議・開発チャットでは、まずこのファイルを読む。
これは現状の要約であり、作業開始・公開・設定変更の許可ではない。

## 目的と今の依頼

- スロットの店舗・機種・台番号の予測を重点的に強化する。実装品質と予測の実用精度は区別する。
- ハイエナ、スマスロ狙い台、ジャグラーは独立した攻略モード。ジャグラーは後に再導入されたため、昔の削除方針で消さない。
- 収集・分析の重点は四條畷駅周辺。長野県の松本・塩尻は現在優先対象外。既存データを削除する意味ではない。
- 3.49「AI工程の採用審査」までローカル実装。次の工程は3.50「朝一の候補画面」。各版は依頼後に一つずつ進む。詳細はリリース記録へ集約し、全ソース集は引き継ぎ時に生成する。

## 確認できた現在地

- ローカルの版：**3.49.1**（リリース日2026-09-21）。3.49.0のPWA配信後、APIのUTC/JST日付境界テスト失敗を修正するパッチ。外部AI実通信・実戦へのモデル切替は未実施。
- 3.49はAI10機能を用途別に審査。開発用評価・誤回答点検・費用管理を採用、説明／傾向／画像／資料検索を補助限定、外部AI接続の実品質／比較選定を保留、AIによる実戦判定変更は不採用。接続設定と品質認定は別。PCのAI分析・スマホ設定に共通審査画面を追加。
- 料金の一括・排他予約、送信前の実行権記録、通信不明時の同じIDでの再確認、同意の条件固定を修正。画像の未確認文字は新規保存・公開せず、既存DBの元候補は削除していない。資料の旧版復活・条件不足も修正。
- `scripts/verify_ai_release.py` が隔離データ・模擬AIで全体を点検し、`data/ai_review/verification_3.49.json` を生成。関連ソース・資料・テストの変更時は審査表示を保留する。コード変更後の認定更新はこの検証を再実行する。テスト成功は実AIの成績ではない。
- 3.41は根拠契約・ID照合・固定文表示・出典展開を実装。自由なAI文章生成は止め、根拠の選択だけをAIへ任せる。外部AIは明示有効化まで停止。個人履歴・会話履歴・推測入力は外部送信しない。
- 3.43は5種AIの共通問題・共通根拠比較を追加。初期値は0通信で、契約適合・一致・速度・費用を分離。外部実測・AI採用・予測変更は未実施。
- 3.44は版管理した架空11問、正解根拠・不要根拠・棄権・反復の採点、追記保存、重複要求防止、進捗表示を実装。実店舗の時点固定データでの評価と外部AI各社の実成績は未計測。
- 3.45はブラウザ内OCR、抽出値の照合・訂正必須、追記専用保存、元画像の任意端末保存、未確認マップ反映を実装。外部画像AIは使わない。
- 3.46は`api/ai_guard.py`で対象・期間・単位・資料内指示などを点検。棄権を一般統計で埋めず、通常回答・比較・評価へ共通適用。旧評価は保持。画像の保存と反映を分離し、古い結果を無効化、マップは別名の未確認下書きにする。
- 3.47は用語8件・機種資料13件・期待値条件24件・周辺店舗参照先8件を版・出典付きで検索。外部通信・追加学習はなく、期待値は登録条件選択後だけ引用。期限外・撤回・競合は除外。機種仕様や店舗の強さを新規認定していない。
- 3.48は外部AIを1回ごとの確認制にし、単価・月上限・個人情報を通信前に点検。本文・生回答・鍵を保存せず、使用量と失敗区分だけ追記記録し、実行IDで重複課金を防ぐ。複数AIは全社の事前点検に通らない限り通信0回。
- 最後の検証：全体Python716件、UI16テストスクリプト、主要JS構文確認成功。既知のhttpx移行警告1件。実画面操作・実iPhone・外部AI実通信は未試験。
- 3.33は直近28日と以前56日を同じ版・実装・店・対象種類・方式で点検。共通対象の全件照合日だけで比較し、データ不足と誤差拡大・観測変化を区別。実台帳の評価対象0、実警告精度は未計測。常時通知・自動判定変更なし。
- 3.32は上位1・3・5件の選択を事前固定し、参考・見送り込み／現行判定通過を分離。共通日の成績と候補不足・未照合を表示。実データの評価対象は0、最適な候補数は未計測・未採用。
- 3.31は現行＋3基準方式の試験補正・予測幅を事前保存して検証。実データは旧条件1日分のみで比較対象0、改善率未計測。機種平均／台番号・版・実装を分離し、旧予測へ補正を後付けしない。
- 3.30は研究結果の再利用と事前成績の審査・追記保存。新プロトコルの事前成績不足で現行方式を維持する。「採用可」も適用済みとはしない。
- 3.26イベント、3.27店舗×機種、3.28台番号、3.29配置・翌日傾向は研究用。本番採用審査は3.30。実装済みでも着席推奨へ採用済みとは限らない。
- 3.29は3店舗で翌日差枚傾向105区分を表示。少数日の記述統計を含む。確認済みマップ・確定設定の連日比較は0で、並び・島の試算も0組。
- 3.28の測定では、対象日2026-09-11・120日履歴・入力期限2026-09-09で台番号の観測対象82台、新順位0台。同機種の複数台を比較できる日数が不足。現在時点の再集計ではない。
- 新方式の台番号的中率・実戦勝率は未計測。テスト成功や版番号を、勝率・完成度の割合に置き換えない。
- 3.49.0はGitHubへpush済みでPWA配信成功、APIは事前テスト失敗により3.10.1を維持。3.49.1で日本時間を統一してPWA／API／EXEを揃える。EXEは照合用ソース不足の場合、審査を保留する。

## 構成・場所

- このPC：`C:\Users\yoshi\OneDrive\デスクトップ\Python\pachi-tool`
- Python/FastAPI：`api/`、分析：`hall/`、収集：`scraper/`、保存：`records/`・`data/`
- PCブラウザ画面：`web/`、iPhone PWA：`mobile/`、Windowsソフト：`desktop/`
- 狙い台検索の連携：`api/routers/hall.py`。新方式：`hall/event_prediction.py`、`hall/machine_prediction.py`、`hall/seat_prediction.py`、`hall/placement_analysis.py`。
- 観測期間：`hall/seat_history.py`、品質監査：`hall/prediction_quality.py`、予測保存：`hall/prediction_log.py`。
- 共通検証画面：`mobile/prediction-verification.mjs`。配置分析・列入力：`mobile/placement-analysis.mjs`。片方の修正だけで全版反映済みとしない。
- 起動・移行・公開URLは [README.md](README.md)。DB、個人の収支、APIキーを公開Gitへ入れない。

## 引き継ぎ時の注意

- 9月12日の確認ではブランチは`main`、リモートは`https://github.com/Yoshirou911/pachi-tool.git`。
- 多数の未コミット変更・未追跡ファイルがある。新方式のコードや更新記録も未追跡。既存変更を消したり、全件を無差別にコミットしたりしない。
- 別PCではローカル未コミット変更やDBは自動で引き継がれない。GitHubに最新があると仮定しない。

## 必要な資料だけ読む

1. [DECISIONS.md](DECISIONS.md)：決定事項と、未確定のモデル案。
2. [TODO.md](TODO.md)：次の会議・実装の入口。
3. [PREDICTION_ROADMAP.md](PREDICTION_ROADMAP.md)：全工程の一覧。
4. [RELEASE_3.49.md](RELEASE_3.49.md)：直近の詳細。過去版は調査対象になった場合だけ読む。

確認用サーバーは全モジュール読込前に`DATA_DIR`をコピー先へ設定する。既存の読込時初期化による元ルールの時刻更新と復元については3.33記録を参照。

変更後は、この要約の版・検証日・配信状態を更新する。長い作業ログを追記せず、詳細は各リリース記録へ残す。
