# v3.46.0：AI回答の混同・断定を防ぐ共通チェック

実装日：2026-09-19。ローカル実装・自動検証済み。次の工程は3.47「PACHI TOOL専用知識」。

## 何が変わるか

- 通常のAI回答は、店舗・機種・台番号・イベント・集計期間・数値と単位を照合してから根拠を渡す。対象と違う根拠は固定説明への復帰でも採用しない。
- AIが根拠を選べなかった場合は「判断できない」と表示する。空回答を一般的な統計へ自動置換しない。
- 確定設定、高設定の的中率、本人の将来の勝率・利益、裏付けのない角台・隣接などは回答を控える。通常回答でこの条件に該当した場合、外部AIを呼ばない。
- 「根拠・出典を見る」で除外数と理由、出典・取得時刻の不足を表示する。出典未記録を架空のURLで埋めない。
- 複数AI比較と固定問題評価にも同じ点検を適用。「形式が正しい」と「根拠が適切」を別の成績として示す。

## 画像読取りの修正

PC・スマホとも「狙い台捜索 → 店内 → 画像を読み取る」を使う。元画像と候補を照合・訂正して保存した後、必要な場合だけ新しい反映ボタンを押す。

- 保存だけではハイエナの数値欄やマップ編集欄を変更しない。反映ボタンに現在の入力／編集中の下書きを置換する旨を表示。
- 写真・画像種類の変更で以前の結果を破棄。数値編集で照合チェックを外す。店舗・日付の変更時は古い結果の保存・反映を止める。
- 読取り中の写真差替え、保存中の連打、保存待ちの間の店舗変更を点検。変更前の条件で保存完了した場合も現在画面へ反映しない。
- APIでは種類の違う値、整数へ変換され得る真偽値・文字列、照合済みを装う数値を拒否。台番号の部分切取りや重複の黙認を廃止。
- マップは画像記録IDを含む別名の未確認下書きにする。同店・同日の既存マップと保存キーを分け、元の出典URL、日付、島・列を引き継がない。最大1,000台も編集領域内に仮配置する。
- 反映したゲーム数がAT間・ボーナス間など何を表すかは自動判別できないため、機種・数え方の確認は引き続き必要。

## 実装と互換性

- `api/ai_guard.py`：根拠の識別情報、質問範囲、期間、指標・単位、URL形式、資料内の誘導指示、未検証の断定を決定的な規則で点検。機種名・店舗名は既存の正規化を使用。
- `api/ai_evidence.py`：対象制約も識別ハッシュに含める。通常回答では除外後の根拠を再固定し、外部AIはそのIDだけを選ぶ。自由文や新しい数値を表示しない既存方針は維持。
- `api/ai_hall_analysis.py`／`api/ai_service.py`：店舗・機種・期間・分析区分の制約を生成元で固定。将来の登録イベント日と、過去の実績期間を区別。
- `api/ai_comparison.py`：比較方式を`ai-provider-comparison-v2-guard`へ更新。形式適合率とは別に不採用数と棄権数を返し、点検で不採用となった応答の報告トークン・設定単価による費用も保持。
- `api/ai_evaluation.py`：採点方式を`evidence-selection-eval-2.0.0`へ更新。`data/ai_eval/suite_v2.json`は既存の架空11問に対象制約を明示した新版。v1を残し、保存済みの完了集計は再採点・上書きしない。
- PC・スマホ共通表示、キャッシュ識別、更新履歴、Windowsビルド用バージョン情報を3.46.0に統一。

## 確認結果

- Python全体 **582件成功**。追加検証は不一致・改変・誘導・断定・単位・期間・出典、棄権、比較の不採用、旧評価の不変、画像入力契約を対象にした。
- JavaScript **14テストスクリプト成功**。画像の非同期競合、明示反映、1,000台下書き、再確認、HTML無害化、PC／スマホの共通読込みを含む。
- PC／スマホのアプリ構文確認と変更差分の空白エラー検査に成功。
- 既知のStarlette TestClientのhttpx移行警告が1件。実ブラウザ・実iPhoneカメラ・EXE・外部AI実通信は未試験。

## 限界と配信状態

この版は誤回答を抑える規則と回帰テストであり、全ての自然文・巧妙な資料内指示を検出する保証ではない。明示された条件と入力内で既知の名前を照合するため、未知の固有名詞、曖昧な日付・言い回しは完全には解釈しない。慎重すぎて回答を控える場合もある。

出典URLの形式点検は出典内容の真偽確認ではない。元データの誤記や未取得履歴、実店舗の正解ラベル不足は解消していない。架空問題の点検結果・テスト件数を実戦的中率や勝率と扱わない。画像文字は人の確認値として保存するだけで、専用知識や確定イベントへ自動採用しない。

GitHub push、公開API／スマホPWA配信、EXE再ビルドは未実施。ローカルサーバーの再起動と画面の再読込みで3.46を利用できる。外部AIへの実通信・課金・キー設定は行っていない。`SOURCE_SNAPSHOT.txt`は3.45時点のまま、引き継ぎ依頼時に再生成する。
