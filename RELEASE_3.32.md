# v3.32.0：候補数と的中率の評価

2026-09-13実装・全体テスト、09-14仕上げ確認。ローカル版の更新日2026-09-14。公開API・スマホPWA・EXEへの配信は未実施。

## 追加した機能

- 上位1・3・5件の選び方と順位を翌日予測と同時保存。既存の順位・着席判定は変更しない。
- 「参考・見送りを含む比較用」と「保存時の現行判定通過だけ」を分離。
- 候補が出た日、算出0の日、候補不足、同点、未照合を残し、件数別の公開差枚プラス率・平均差枚を表示。
- 各Kの候補を全件照合した成績と、候補母集団全件を照合した共通日での比較を別表示。
- 共通日は上位1・3・5件と「順位で絞らない母集団」を同じ日で比較する。旧予測の後付けルール集計は研究区分に分け、事前成績へ合算しない。
- PC・スマホ共通画面から、過去の評価対象機種・台番号と結果も確認可能。

入口：ホーム → 狙い台捜索 → データから狙い台を探す → **候補を何件に絞る？（3.32）**。
新しい記録は既存の「明日の予測を固定・結果を照合」／稼働中サーバーの定期保存で蓄積する。

## 固定条件と限界

| 項目 | 条件 |
|---|---|
| 対象 | 四條畷周辺。機種平均／台番号、版・実装・方式・対象の絞り込みを分離 |
| 比較方式 | 現行・単純平均・直近14日平均・曜日平均。4方式が揃った同一対象だけ |
| 順位 | 予測差枚降順→確率降順→店舗・機種・台番号昇順。結果で再選択しない |
| 判定通過の絞り込み | 保存時の現行判定を4方式共通で利用。別方式の実戦推奨を新設するものではない |
| 件数 | 上位1・3・5件。足りない日は存在する分だけ選び、不足日と実際の選択件数を明示 |
| 正解 | 選んだ対象の公開差枚が0枚超。±0は非プラス。高設定的中率・本人勝率ではない |
| 重み | 日ごとの選択候補プラス率・平均差枚を計算し、日を等重みにして平均 |
| 欠損 | 上位の結果不明を下位で差し替えない。一部でも未照合ならその日の成績を集計しない |
| 共通日 | 順位で絞る前の母集団の全結果が必要。同じ日は各Kと母集団平均で共通 |
| 期間 | 昨日まで84暦日。未保存・対象外日は見送り成功に数えない |
| 日数点検 | 候補全件照合30日・8週、選択対象の照合率80%以上、直近14日内の全件照合 |
| 保存保護 | 順位ルール・実装・元予測との一致を検査。異なるルールや矛盾は除外し、問題があれば保留 |

日数条件を通過しても「参考評価のみ」。最適Kを自動選択したり、採用や将来利益を保証したりしない。複数比較・時系列相関・掲載の偏りが残る。全結果を必要とする共通日にも完全に掲載された日への偏りがあり、稼働全体の代表とは限らない。
機種平均と個別台は違う指標。ここでの平均差枚は投資額・交換率・遊技時間を再現した利益ではない。
3.28の研究順位・現在画面の総合順位とは別の比較用順位であり、そこへの新順位の適用は行わない。

## 実装・保存・API

- `hall/candidate_evaluation.py`：順位固定、候補数別集計、共通日比較、事前／後付け区分。
- `hall/prediction_log.py`：既存の不可変ペイロードに `candidate_evaluation`（条件・順位・実装識別）を追加。旧予測を上書きしない。
- `hall/probability_validation.py`の台帳読取を再利用。時点・対象・結果証拠、±0・欠損・稼働不明／1000G未満の扱いを共通化。
- `GET /api/predictions/candidate_evaluation`：固定条件の読取集計。検索・学習・保存・答え合わせを実行しない。任意K・地域・予測値・結果のクライアント指定は使わない。
- `mobile/candidate-evaluation.mjs`を共通検証画面へ追加。横スクロール表、内訳・固定候補の開閉、通信失敗時の再確認。自動ポーリングなし。
- DB新テーブルなし。元の予測・対象・結果台帳を利用。

## 確認結果・現在地

- 全体Python **428件成功**（34.14秒）、新規23件。関連テストを先に実行し、全体は最後に1回。
- JavaScript **7テストファイル成功**。PC／PWAのJS構文チェック成功。
- 同点・入力順、4方式の同一対象、候補不足、現行判定通過の分離、結果不明時の差替え禁止、±0、部分照合、共通日、日別重み、日数・鮮度・照合率、保存順序の矛盾、異なる方式、事前／研究分離、冪等性・元予測保護、API・XSSを検証。
- 実データコピーでは2026-06-21〜09-12に、比較条件未保存の旧バッチ1日分のみ。評価可能な保存0日・対象外を含む未評価84暦日。候補数別の実プラス率は未計測。過去実績DB全体が0という意味ではない。
- 9月14日にも実データの空表示を確認（対象期間6月22日〜9月13日、比較条件未保存1日分・評価対象0）。その後、検証用コピーにのみ「画面テスト用・架空成績」と明記した2日分を追加し、0枚と結果待ちの比較表を確認。テスト数値を実測成績として報告しない。
- スマホ390pxではページ全体の横はみ出しなし、表は287pxの表示枠内で480pxの内容を横スクロール可能。結果更新・開閉を確認。定期収集・予測は停止した確認サーバーを使用し、元DBを変更していない。
- PC1280pxでも架空成績の比較表・開閉・0枚／未照合の表示を確認。PC・スマホとも確認中のブラウザー警告／エラーなし。
- 既存依存の非推奨警告1件は残存。テスト失敗ではない。

GitHub push・公開配信・EXE再ビルドは未実施。全ソース集は再生成しない。
次は **3.33：店舗傾向の変化・予測劣化の監視**。
