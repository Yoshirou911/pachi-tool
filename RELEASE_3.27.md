# v3.27.0 店舗×機種の予測比較

完了日：2026-09-10。改訂予測特化工程の3.27のみ。3.28には進んでいない。

## 実装

- 店舗×スマスロ機種ごとに、その機種を除いた同店の他機種と比較する研究用モデルを追加。モンキーターンだけでなく、取得済みの全スマスロに同じ規則を適用する。
- 店舗・機種の別名を統一し、重複・矛盾・差枚欠損・台数欠損を監査。平均1000G未満と稼働不明は使用しない。未収集・不足をゼロ差枚や弱い店と扱わない。
- 同日の他機種2種類以上を台数で加重平均する。対象機種自身は比較対象に入れず、標本数は台数ではなく日数で数える。
- 対象日の2日前まで、前56日内の比較可能日を使用。10日未満、または最終比較日が入力期限より14日超古い場合は研究用予測も出さない。
- 他機種の過去同曜日平均（3日未満なら同期間平均）に、対象機種と他機種の平均差を `n/(n+20)` で縮小し±500枚に制限した値を足す。対象日の他機種実績は使わない。
- 比較用の基準は、同じ学習日の対象機種の単純平均。既存の本番予測モデルとの比較ではない。
- 直近最大60実績日を日ごとに過去へ戻して試算し、両方式を同じ評価日で平均絶対誤差比較する。都合のよい予測だけを選ばず、予測不能日も数える。評価20日未満は検証不足。
- 入力期限以前の21日以内の設置一覧がある場合は照合。一覧にない機種の現在予測を停止するが、過去の比較結果は消さない。将来の設置情報を過去に混ぜない。
- 3.30の採用審査までは研究用。現在の店舗・機種順位や着席判定には加点しない。今回の測定結果を見て閾値を調整していない。
- スマホ・PC共通の「店舗×機種の比較（3.27・研究用）」欄に、店舗→機種→予測・比較誤差・不足理由・設置状態・出典を表示。画面は直近10件の答え合わせと最大5出典に絞り、APIと固定ペイロードには最大60件の試算・全出典を残す。
- 既存の前日固定処理に新しい比較結果を同梱。旧予測・元実績・旧比較レポートを上書きしない。DB構造変更はない。
- 版番号・更新履歴・キャッシュ版を3.27.0に統一。

## 検証

- Python全314テスト成功。既存のStarlette/httpx非推奨警告1件。
- 共通検証UIのJavaScript契約テスト・構文チェック成功。
- 自身の機種を除外する比較、台数加重、未来データの遮断、対象日の他機種欠損、重複・矛盾・稼働欠損、標本不足、設置一覧の時点制限、古い履歴、固定ペイロードの不変性を検証。
- スマホ幅390pxで実データの店舗・機種詳細の表示と開閉を確認。PC版でも共通パネルの表示・開閉を確認し、両画面のブラウザエラーログはなし。
- 自動収集・前日固定を起動しない検証サーバーを使用。実データの新しい予測を手動固定する操作は行っていない。

## 実データの測定結果

条件：四條畷駅周辺、対象日2026-09-11、直近120日、70%モード。3.27の入力期限は2026-09-09。

| 店舗 | 店舗×機種数 | 20日以上の誤差比較 | うち基準平均より誤差が小さい組 | 現在の研究用予測あり |
|---|---:|---:|---:|---:|
| ひま・わり四條畷店 | 0 | 0 | 0 | 0 |
| キコーナ四條畷店 | 0 | 0 | 0 | 0 |
| キコーナ大東店 | 0 | 0 | 0 | 0 |
| キコーナ野崎店 | 0 | 0 | 0 | 0 |
| スーパーコスモプレミアム大東店 | 60 | 46 | 26 | 37 |
| ニコニコ住道店 | 41 | 13 | 7 | 6 |
| ベガスベガス大東店 | 76 | 59 | 32 | 48 |
| マルハン大東店 | 44 | 24 | 17 | 0 |
| 合計 | 221 | 142 | 82 | 91 |

221は店舗×機種の組数で、異なる機種が221種類という意味ではない。82/142は比較用機種平均より過去の誤差が小さかった組数であり、的中率・勝率ではない。91件も着席推奨ではない。0件の店舗は比較材料不足で、回収店という判定ではない。

## 限界と次工程

- 公開対象機種・公開日・稼働の偏りがある。他機種との差は機種の優遇や設定投入の因果効果を証明しない。
- 結果の公開時刻・改訂履歴は完全には復元できない。今回の比較は研究用の過去検証で、本番の事前予測成績とは別。
- 誤差が小さかった組の統計的有意性、多数比較による偶然、前向き検証、既存本番モデルとの比較は未評価。本番採用や精度向上を保証しない。
- 新しいデータ収集は今回行っていない。古いデータだけの店舗には現在予測を出さない。
- 高設定的中率・本人の勝率や収支・実物の同一台を予測できた意味ではない。
- 次は3.28「台番号ランキング」。新方式の比較・採用審査は3.30。

## 配信

ローカル実装のみ。GitHub push、公開API／GitHub Pagesへの配信、EXE再ビルドは未実施。公開スマホ版・配布済みEXEが更新された意味ではない。
