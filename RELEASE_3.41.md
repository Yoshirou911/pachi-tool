# v3.41.0：根拠付きAI回答

実装日：2026-09-14。ローカル実装。次は3.42「店舗特化AI分析」。

## 何が変わったか

- 共通契約 `api/ai_evidence.py` に対象日、店舗、機種、台番号（根拠がなければnull）、イベント、集計期間、数値、単位、出典URL、元データ取得時刻、不足情報を固定。内容のハッシュと根拠IDを付ける。
- AIの出力は `{snapshot_id, claims:[{evidence_id}]}` のみ。自由文、数値、追加キー、不明ID、重複、別スナップショット、空回答、JSON不正を拒否する。回答文は固定された根拠からサーバー側で作る。
- 指示文による抑制だけだった旧自由回答を置換。チャット、レポート、店舗カルテ、イベント予測・質問、設定推測コメントで共通形式を返す。旧 `reply`・`report`・`comment`・`summary` も残して既存画面と互換にする。
- AIの選択順で統計の順序を変更しない。イベント判定・品質不足は、AIが根拠を選ばなくても表示する。根拠不足や不正応答は固定説明へ戻す。予測やDBの値は変更しない。
- PC・スマホ共通の `mobile/ai-evidence.mjs` に「根拠・出典を見る」を追加。期間、単位、URL、取得時刻、未記録、入力値であることを展開表示。HTMLと危険なURLを表示用に無害化する。
- 一般チャット／レポートは読み取り専用の公開台実績を使用。対象日当日以後の結果、別店舗、台番号なし／0番の集計行を除外。台番号の現在配置を裏付ける根拠は作らない。
- イベント質問を1要求1回のAI呼出しへ整理。台番号質問や根拠なしの場合は外部AIを呼ばない。
- 外部送信を既定停止へ変更。既存APIキーがあっても `PACHI_AI_ALLOW_EXTERNAL=true` の明示が必要。接続クライアントを直接使っても停止を守る。
- 個人実戦履歴・過去の会話は根拠として採用せず外部送信しない。設定推測の入力値もローカル説明のみ。利用者が入力した質問文自体は、外部AI有効時には送信される。
- Windows版の数値版情報に残っていた3.33も3.41へ合わせた。

## 契約の境界

AIは関連する既存根拠を選ぶ役割で、自由に文章を生成する機能ではない。関係ない根拠を選んだ場合でも数字・判定を捏造できない構造を優先した。関連性・説明の自然さ・店舗特化の分析は3.42以降で拡張する。

一般チャットは対象日以前の直近30日、記録日数・件数順で最大30店舗×機種区分。回答本文は最大8根拠、根拠欄は抽出した全件を表示。元URLは区分ごと直近10件の抜粋で、抜粋であることを表示する。これは好調機種の予測ランキングではない。

既存カルテ／イベントAPIが元取得時刻を持たない場合、説明生成時刻で代用せず未記録とする。集計期間も欠けた場合は未記録。元DBに取得・改訂履歴が不足しているため、過去日を指定しても当時の情報だけで作った厳密な事前予測とは認定しない。

推測コメントは入力値の説明で、再推定・入力値の独立検証ではない。従来の自由回答にあった投資額、続行判断、確定設定の推定はAIから生成しない。元の設定推測画面を使う。

## 検証

- Python全体476件成功。うちAI関係31件で5社の共通契約、模擬HTTP、改変・不明根拠の拒否、判定保持、個人履歴を送らないこと、二重通信解消、日付・店舗・集計行の除外、API互換を検証。
- 既知のStarlette TestClient/httpx移行警告1件あり。
- AI根拠表示、モバイルUI契約、モバイル計算のJavaScriptテスト成功。PC・スマホJSとService Workerの構文確認成功。
- 実機／ブラウザでの画面操作は未実施。各社の有料APIによる実回答は未試験。模擬応答での契約検証と区別する。

## 配信状態

外部AIへの実通信、契約、課金、APIキー登録、公開設定の変更はしていない。GitHub push、公開API／PWA配信、EXE再ビルドも未実施。ローカル起動／再読み込み後に3.41を利用できる。

次は3.42。出典付きの曜日・イベント・機種・配置の説明範囲を広げる。3.41のテスト成功を的中率や実戦利益の改善として扱わない。
