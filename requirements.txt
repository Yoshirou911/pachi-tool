# pachi-tool 依存パッケージ
# pip install -r requirements.txt

# スクレイピング
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
curl-cffi>=0.7.0        # Cloudflare自動突破（Chrome TLSフィンガープリント模倣）
cloudscraper>=1.2.71    # curl-cffi未対応環境用フォールバック

# スケジューラー（サーバーサイド定期スクレイプ）
apscheduler>=3.10.4

# API サーバ
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Pydantic (fastapi 同梱だが明示)
pydantic>=2.0.0

# AI分析
groq>=0.11.0
