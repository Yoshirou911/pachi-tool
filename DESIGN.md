# パチスロ統合分析ツール 設計図

## 画面間の機能同期ルール

- `web/` はPCブラウザ版とWindowsソフト版で共通利用する。
- `mobile/` はiPhoneのPWAアプリ版として利用する。
- 新しい主要機能・メニュー・用語変更は、原則として同じ変更内で `web/` と `mobile/` の両方へ反映する。
- 端末固有の制約で同じ機能を提供できない場合は、片方を黙って省略せず、画面上に利用条件を明示する。
- 起動時は両画面とも「ホーム」から始まり、「ハイエナ」と「狙い台捜索」を独立した攻略モードとして選択できる状態を維持する。
- 「狙い台捜索」は蓄積した店舗・曜日・機種データによる候補抽出を主機能とし、手入力はデータにない候補を補う二次機能にする。
- ハイエナ用メニューと狙い台捜索用メニューは混在させず、ホームを経由してモードを切り替える。

設定推測・収支管理・店傾向分析を1つに統合し、Python で本格的なデータ分析をしつつ、スマホ/PC どちらからでも使えるツールの設計。Claude Code で段階的に実装していく前提でまとめている。

---

## 1. 基本コンセプト

精度の核は**ベイズ推定**。3つの機能はバラバラではなく、ベイズの枠組みで一本に繋がる。

```
店傾向分析  ──(事前分布を生成)──▶  設定推測エンジン  ──▶  期待値/撤退判断
     ▲                                      │
     └──────────(推測結果を蓄積)◀────────────┘
                       収支・履歴管理
```

- 設定推測エンジンが各台の事後分布(設定1〜6の確率)を出す
- その結果を履歴に蓄積する
- 蓄積データから「この店のこの条件では高設定が出やすい」という**事前分布**を学習する
- 学習した事前分布を次回の推測に注入すると、同じ観測でも精度が上がる

これが「全部入りにすると精度が上がる」理由。3機能が循環してデータが効いてくる。

---

## 2. 技術スタック(スマホ/PC両対応の実現方法)

「Pythonで本格分析」と「スマホでもPCでも」を両立する王道構成:

| 層 | 技術 | 役割 |
|---|---|---|
| 推定コア | Python(numpy/scipy) | ベイズ推定・統計分析。**完成済み: `core/bayes_engine.py`** |
| API | FastAPI | コアをHTTP経由で叩けるようにする |
| DB | SQLite(将来Postgres) | 機種マスタ・実戦履歴・店データ |
| フロント | レスポンシブWeb(React等) | スマホ/PC共通のUI。ブラウザで動くので両対応が自動で実現 |

ポイント: **ネイティブアプリを2つ作らない**。レスポンシブWebなら1つのコードでスマホ・PC両対応になり、入力もスマホで完結する。重い分析はサーバ側のPythonが担当する。

---

## 3. モジュール構成

> 以下は2026-08-11時点の実際の構成。ロードマップ通りに育ち、当初想定より大きく拡張されている
> (ハイエナ/期待値機能・スマホPWA・デスクトップ版・外部データ収集・店舗稼働率トラッキング等)。

```
pachi-tool/
├── core/
│   ├── bayes_engine.py        設定推測の心臓部(ベイズ推定エンジン、機種非依存)
│   ├── cli.py                 CLIツール
│   └── setting_change.py      設定変更・宵越し判定
├── data/
│   ├── machines/               各機種の確定データ(設定推測エンジン用)
│   └── opportunity_catalog.json 期待値プロファイルのカタログ(ハイエナ機能用)
├── records/
│   └── models.py               収支・実戦履歴(sessions.db)
├── hall/                       店舗分析・店舗運用ドメイン
│   ├── prior.py                 履歴から事前分布を学習
│   ├── machine_scope.py         機種の対応範囲判定
│   └── occupancy.py             店舗稼働率トラッキング・候補店ランキング(hall_reports.dbに保存、v2.5.0〜)
├── opportunity/                 ハイエナ(期待値)機能ドメイン
│   ├── models.py                 プロファイル/候補/実戦結果のDB(opportunities.db)・判定ロジック
│   └── machine_models.py         機種別の消化時間モデル・ツラヌキ補正(calculate_machine_adjustment)
├── value/
│   └── ev.py                    事後分布×機械割で期待値を計算
├── scraper/                     外部データ収集(すべて非公式スクレイピング、規約に注意)
│   ├── anaslo.py / anaslo_batch.py  アナスロ(台番別データ・店舗マスタ scrape_hall_config)
│   ├── minrepo.py / minrepo_archive.py みんレポ(機種別差枚・過去データの中断再開収集)
│   ├── events.py                 ホールイベント情報
│   ├── pachiseven.py / bulk.py / chonborista.py / anoslot_public.py / pworld_snapshot.py  各種データ源
│   └── opportunity_crawler.py    公開期待値表の週次確認・承認キュー(opportunity/のプロファイル更新用)
├── api/
│   ├── main.py                   FastAPIアプリの組み立て・ミドルウェア・起動処理のみ
│   ├── deps.py                   共有ヘルパー(ロガー・キャッシュ・DB接続・パス解決)
│   ├── scheduler.py              夜間スクレイプの状態管理・APScheduler
│   ├── ai_service.py             Groq経由のAIチャット/レポート
│   └── routers/                  機能ごとのエンドポイント(1ファイル=1 URLプレフィックスの原則)
│       ├── machines.py opportunity.py estimate.py sessions.py hall.py scrape.py
│       ├── events.py map.py occupancy.py layout.py admin.py ai.py version.py
├── web/                         PCブラウザ版・Windowsソフト版で共通利用するレスポンシブフロント
├── mobile/                      iPhone PWA版(GitHub Pagesで配布、web/とは別実装・機能同期ルールは冒頭参照)
├── desktop/                     Windowsデスクトップ版(WebView2+PyInstaller)のパッケージング関連
├── app_version.py               全クライアント共通のバージョン・パッチノート(正本)
└── tests/                       pytest(Python) + node実行のUI契約テスト(.mjs)
```

---

## 4. 各モジュールの中身

### records(収支・履歴管理)
1回の実戦につき記録する項目: 日付、店、機種、台番号、総ゲーム数、各要素の観測回数、投資、回収、差枚、推測した事後分布。これが全分析の燃料になる。台番号の末尾・角台かどうか・曜日・イベント日フラグなども一緒に持たせると、後で店傾向が読める。

### hall(店傾向分析・店舗運用)
履歴を条件(店×曜日×イベント日×機種×台位置 等)でグループ化し、各条件での「高設定が出た割合」を集計して**事前分布**を作る(`prior.py`)。サンプルが少ない条件は一様分布に近づける(ベータ分布などで平滑化)と過学習を防げる。出力は `bayes_engine.estimate(obs, prior=...)` にそのまま渡せる形。`occupancy.py` は店舗の稼働状況(高/中/低・任意の平均回転数)を記録し、店舗×曜日×3時間帯の混雑を推定する。さらに対応スマスロ数・過去稼働・混雑バランス・データ鮮度・サンプル数を合成し、今から巡回する候補店を理由・警告つきで順位付けする。

### opportunity(ハイエナ・期待値機能)
店の実機データに頼らず、公開されている期待値表(なな徹等)を機種別プロファイルとして持ち、現在値・交換率・資金状況から「打つ候補か/見送りか/資金不足か」を判定する(`assess_quick_decision`)。`machine_models.py` が消化時間モデルと機種固有の補正条件(からくりサーカスのCZスルー等、根拠が確認できたものだけ)を持つ。`opportunity_crawler.py`(scraper配下)が週次で公開表との差分を検知し、人間の承認を経てプロファイルを更新する。

### value(期待値・撤退判断)
事後分布と設定別機械割(`machine_kw`)を掛けて期待値を出す。「今の推測なら回し続けるべきか撤退か」を判定。**ツールの実利益はここが大きい** — 高設定を当てる以上に、低設定での粘りを止めて損失を減らす効果が効く。

### api / web / mobile
FastAPI(`api/`)で各モジュールをエンドポイント化し、`web/`(PC・Windowsソフト版共通)と`mobile/`(iPhone PWA)の両フロントから叩く。スマホでカウントを入力 → サーバで推定 → 結果を即表示、という流れ。`api/main.py`自体はアプリの組み立てのみを担い、実際のエンドポイントは`api/routers/`配下に機能ごとに分割されている(1ファイル=1 URLプレフィックス)。

---

## 5. 精度を最高峰にするための要点

1. **多要素同時推定** — 要素が増えるほど事後が尖る。実装済み。記録できる設定差要素はできるだけ多く取る。
2. **正確な理論値** — `data/machines/` の数値の正確さが精度の上限を決める。ここが甘いと他が完璧でも精度は出ない。
3. **店傾向を事前分布に注入** — ベイズの最大の強み。同じ観測でも事前が効けば精度が上がる。
4. **設定変更・宵越し対応** — 朝イチからのデータか、途中からの拾いデータかを区別して扱う。
5. **平滑化** — サンプルの少ない店条件は事前を一様寄りにして過信を防ぐ。

---

## 6. 実装状況(2026-08-11時点)

当初のロードマップ(推定エンジン→履歴→期待値→事前分布→API→Web→循環の完成)は完了済み。
現在は以下のフェーズに拡張されている:

- ✅ 設定推測の循環(履歴→事前分布→推測→履歴)
- ✅ ハイエナ/期待値機能一式(opportunity/) — 判定・資金管理・公開表の週次自動確認
- ✅ 外部データ収集(scraper/) — アナスロ・みんレポ中心、店舗マスタ管理
- ✅ スマホPWA(mobile/)・PC/Windowsソフト版(web/, desktop/)の3クライアント運用
- ✅ 店舗稼働率トラッキング(hall/occupancy.py, v2.5.0〜) — 高/中/低と平均回転数を現地記録
- ✅ 稼働率の統計的予測とハイエナ候補店ランキング(v2.6.0〜) — 曜日×3時間帯、信頼度、設置・稼働・鮮度を合成
- 🔜 対応エリア拡大 — アナスロ/みんレポとも大阪府中心のため、大阪府外の店舗はスクレイピング非対応(稼働率の手動記録は地域を問わず利用可能)

直近の変更履歴は `app_version.py` の `PATCH_NOTES`、詳細な設計判断はgitのコミットメッセージを参照。

---

## 7. 現実的な前提(重要)

このツールは「必ず勝てる」装置ではない。最終的な勝敗を決めるのは推測精度ではなく、**その店が高設定をどれだけ使っているか**。高設定が存在しない日はどれだけ精密でも勝てない。

ツールの本当の価値は2つ:
- **低設定を早く見切って損失を減らす**(撤退判断)
- **データで店の傾向を掴み、勝負所を絞る**(事前分布)

期待値プラスの立ち回りは可能だが、それは「精密な推測 × 高設定を使う店の発見 × 規律ある撤退」の合わせ技として成立する。ツールは判断を助けるが、規律を守るのは人間の役目。資金管理を決め、負けても深追いしないルールを最初に決めておくこと。
