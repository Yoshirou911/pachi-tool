# v3.31.0：予測確率と予測幅の検証

2026-09-13。ローカル実装。公開PWA・API・EXEには未配信。

## 今回できるようになったこと

- 現行方式（既存補正を含む）・単純平均・直近14日平均・曜日平均の確率を、機種平均／台番号、版・実装ごとに分けて検証。
- 予測確率帯ごとの予測値と実際のプラス率を棒で表示。件数と独立した評価日数を併記。
- 過去に判明した結果だけで作った試験補正・予測幅を、翌日予測と同時に保存する。
- 同じ保存対象で補正前後のBrier誤差を比較。80%目標の予測幅について、幅内率・平均幅・外れを含む総合誤差を表示する。
- 既存の幅も今後の機種予測から保存し、両方の幅が保存されている対象だけの比較を別表示する。
- 共通画面から更新可能。表示・更新では再検索、学習、答え合わせ、データ収集を開始しない。

入口：ホーム → 狙い台捜索 → データから狙い台を探す → **予測確率と予測幅の検証（3.31）**。
新しい前日保存は、既存の「明日の予測を固定・結果を照合」または稼働中サーバーの既存定期実行で行う。今回テスト用の予測は元DBへ保存していない。

## 時点と固定条件

| 項目 | 条件 |
|---|---|
| 正解 | 公開実績の機種平均差枚／台番号差枚が0枚超。±0は非プラス。高設定・本人収支ではない |
| 学習 | 直近120日。予測開始前に答え合わせ済み、かつ入力期限以前の対象日だけ |
| 分離 | 同じ版・予測実装・方式・対象種類だけで学習。異なる版を合算しない |
| 確率補正 | 全体30日以上、対象の20%幅の確率帯に10日以上。元の確率を10日相当の事前値として縮小補正 |
| 日別重み | 対象→店舗日平均→日平均。同店の多数台を別々の独立日とはしない |
| 試験幅 | 30日以上の日別最大絶対誤差を昇順にし、ceil((日数+1)×0.8)番目を半幅とする。点予測±半幅 |
| 評価 | 昨日まで84暦日。補正前後30日・8週、補正保存対象の照合80%以上、最新照合14日以内、プラス・非プラス各5日以上 |
| 表示判定 | 前半・後半の両方でBrier改善なら「参考改善あり・実戦未適用」。不足・不改善は保留 |
| 幅の総合誤差 | 幅の長さ＋幅外までの距離×10。広げるだけの見かけの改善を確認できるようにする |
| 欠損 | 未照合・遅れて判明した結果・稼働不明／1000G未満を未照合として区別。0枚で埋めない |
| 保護 | 保存・入力時点、ペイロードハッシュ、対象名・番号・日付と結果証拠を照合。不整合の対象は除外し評価保留 |

これらは固定した試験ルールであり、統計的優位性・80%被覆・将来利益の保証ではない。時系列相関、公開対象の偏り、モデル横断の繰り返し確認の影響が残る。予測幅は日別最大誤差を使うため広めになる。

## 未適用・未計測として残すもの

- 試験補正は保存・検証のみ。既存の確率補正ロジック、実戦確率、順位、着席許可は変更しない。3.30の採用審査や切替・復帰の代わりにはしない。
- 3.26〜3.29のイベント／機種研究／台順位／並びの新モデルには、同一定義の確率出力がない。今回の4比較方式と混同せず、それらの校正は対象外。
- 旧版の固定予測に試験補正や予測幅を後付けしない。既存の台番号方式は予測幅を出していないので既存幅は未計測、試験幅のみ今後の保存対象。
- 版・実装を変えると学習の対象が分かれる。頻繁な開発中に異なるコードの実績を混ぜず、同じ実装を維持した前日保存・答え合わせを継続する必要がある。
- 個別店舗・個別機種の校正は今回未実装。対象種類・方式ごとの検証であり、全機種が同じ精度であることを示さない。

## ファイル・依存関係

- `hall/probability_validation.py`：台帳読取 → 既知実績だけの補正・幅計算 → 事前保存値の誤差・幅内率評価。
- `hall/prediction_log.py`：`freeze_comparison` / `freeze_selection` の後で試験補正を計算し、同じ不可変ペイロードに `probability_validation` を追加。条件・学習日数・入力根拠ハッシュ・補正器識別も保存。
- `api/routers/hall.py`：機種の既存予測幅を保存対象へ引き渡す。推奨判定はそのまま。
- `api/routers/predictions.py`：`GET /api/predictions/probability_validation`。サーバー所有の固定期間、結果の読取集計だけ。クライアントの予測・正解・期間指定を受けない。
- `mobile/probability-validation.mjs` → `mobile/prediction-verification.mjs`：PC・PWA共通の結果表示、確率帯の棒、未計測・未適用表示、通信失敗時の再確認。
- DBの新テーブルなし。既存の予測バッチ・対象・結果台帳を利用し、旧レコードの上書きはしない。

## 確認結果

- 全体Python **405件成功**（35.32秒）。新規28件。関連テストを先に行い、最後に全体1回。
- JavaScript **6テストファイル成功**、PC・PWAのJS構文検証成功。
- 時点制限、遅延公開、地域・版分離、日数不足、多数台の重み、±0、欠損・低稼働、前後半悪化、鮮度、異なる補正器の除外、非有限値、既存保存の冪等性、読取API、XSS・未計測表示を検証。
- 実DBのコピーでは評価期間2026-06-21〜09-12に比較条件未保存の旧バッチ1日分のみ。対象0のため、補正改善率や実戦精度は未計測。これは収集済みの過去実績全体が0という意味ではない。
- スマホ390px／PC1280pxで実データの未計測表示・結果更新・版表示を確認。両画面のコンソール警告・エラーなし。確率帯や幅の数値あり表示はテスト用データによるJSテストで確認。確認サーバーは元DBのコピーを使用し、定期収集・予測は停止。
- 既存依存の非推奨警告1件。テスト失敗ではない。

GitHub push、公開版への配信、EXE再ビルドは未実施。全ソース集は節約方針どおり再生成しない。
次は **3.32：候補数と的中率の評価**。
