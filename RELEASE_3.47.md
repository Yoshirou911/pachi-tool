# v3.47.0：専用知識を条件・出典付きで検索

実装日：2026-09-19。ローカル実装・自動検証済み。次の工程は3.48「費用・個人情報管理」。

## 使い方

PC・スマホとも「狙い台捜索 → 店舗×強い機種（下部タブは『傾向』）→ 専用知識を調べる」を開く。上の行く日を使い、資料の種類と機種・条件を選んで検索する。店舗参照先では上で選んだ店舗を使う。

- 用語・使い方：期待値、交換条件、リセット、閉店欠損、ゲーム数、見送り、再プレイ、専用知識の説明8件。
- 機種の登録資料：同梱カタログの確認済み条件がある13機種について、収録状況を表示。全現行機種の仕様を網羅したものではない。
- 期待値条件：確認済み表示の24条件。資料の条件を選択するまでは数値を引用しない。開始値と同じ曲線点に期待値が登録されているものだけを使用する。
- 店舗の参照先：四條畷周辺8店舗のコードに登録された収集先。情報が現在取得できる保証や、出玉・開催の認定ではない。実績の傾向は既存の店舗質問・分析と分離する。

結果には資料ID・版・引用元・確認日・登録基準日・適用条件を表示する。確認日から30日超は注意を表示し、確認日未記録や有効期間未記録は補完しない。検索中の条件変更では古い結果を表示しない。資料の集合が更新された場合は一覧を更新して選び直す。

## 実装

- `api/ai_knowledge.py`：読み取る入力を固定したローカル検索。正規化した語句と明示的な店舗・機種・条件で選ぶ。外部埋め込みAPI、LLM通信、追加学習、DB書込みはない。
- `data/knowledge/documents_v1.json`：版付きのアプリ内用語。外部専門記事ではなく、プロジェクトの方針・入力契約を引用元として明示。
- 期待値は`data/opportunity_catalog.json`だけから変換。`opportunities.db`や個人設定を読まず、読込時のルール再初期化・機種補正・補間も行わない。参考扱いの条件、開始値の裏付けがない条件は検索数値へ採用しない。
- 店舗参照先は`hall/collection_sources.py`の登録設定を使用。URLの新規推測やアクセスはしない。
- 各資料に内容ハッシュ、資料集合に識別ハッシュを付け、引用は`api/ai_evidence.py`の共通根拠形式で固定。`api/ai_guard.py`に専用資料の版・出典・適用条件の点検を追加。一般的な高設定断定・勝率・台番号・イベント根拠の拒否方針も維持。
- 未来の登録・確認、適用期間外、撤回、同じ版の競合、同一条件の内容競合を除外。新しい版が期限外・撤回でも古い版へ戻さない。不正な資料は同じIDの以前の版も隔離する。
- 交換・投資・リセット・計数項目が質問と明示的に矛盾する場合は引用を控える。現在の着席可否はこの検索で判定しない。
- GET `/api/ai/knowledge`：版、件数、選択肢、読込失敗・除外の状況。
- POST `/api/ai/knowledge/search`：版の一致を確認して検索。不一致は409、入力契約違反は422。一般チャットの履歴や画像データは入力として受け付けない。
- `mobile/ai-knowledge.mjs`：PC・スマホ共通画面。既存の根拠表示で引用元を展開。画像抽出記録、会話、個人の実戦記録、APIキーは知識へ取り込まない。
- PC・スマホの資材・キャッシュ・更新履歴とWindowsビルド用バージョン情報を3.47.0へ更新。ビルド設定に専用資料ディレクトリを追加。

## 確認結果

- Python全体 **615件成功**。新規33件は検索・引用、条件の混同、時点・期限・撤回・競合、不正資料の隔離、引用改変、APIの版不一致、通信しないことを検証。
- 固定検索問題 **12件すべて期待する資料IDに一致**。`data/knowledge/retrieval_cases_v1.json`で人が定義した期待IDと照合。取得コード自身から正解を生成しない。
- JavaScript **15テストスクリプト成功**。専用知識のHTML無害化、条件の初期化、遅延応答の無効化、版不一致後の再読込み、PC／スマホ共通資材を含む。
- PC／スマホのアプリ構文確認と差分の空白エラー検査に成功。既知のStarlette TestClientのhttpx移行警告1件。

## 限界と未実施

これは専用資料を検索・引用する層であり、AIモデルの重みの追加学習、LLMによる自由な回答、任意文書の自動読込みではない。固定語と明示フィルターによる検索なので、未知の略称や曖昧な自然文を完全には解釈しない。合う資料がないときは不足表示になる。

期待値は以前の登録資料の数値をそのまま引用したもので、今回ネット上で再検証していない。当日の消化時間・交換条件・現金ギャップ・閉店欠損は再計算しない。店舗参照先の確認日は未記録。情報の鮮度・出典の真偽・実店舗の正解不足は別途確認が必要。

資料の版を識別できるが、カタログ・収集先コードの過去ファイルを自動保管する機能ではない。登録基準日はこの検索層へ追加した日であり、情報を当時知っていた証明には使わない。過去の予測検証をこの検索で再現したと扱わない。将来、手動資料を更新する場合は旧版を残し、登録日・版・有効期間を明示する。

12問一致は検索の回帰点検であり、資料の真偽や店舗予測の的中率・勝率ではない。予測順位・着席判定は変更していない。

実ブラウザ・実iPhone・EXEでの手動確認、外部AI実通信、GitHub push、公開API／スマホPWA配信、EXE再ビルドは未実施。ローカルサーバー再起動・画面再読込みで利用する。検索API未接続の静的PWAではこの検索は利用できない。`SOURCE_SNAPSHOT.txt`は3.45時点のまま、引き継ぎ依頼時に再生成する。
