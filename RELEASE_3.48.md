# v3.48.0：外部AIの費用・個人情報管理

実装日：2026-09-20。ローカル実装・自動検証済み。次の工程は3.49「AI工程の採用審査」。

## 変更内容

- 通常の店舗質問、複数AI比較、固定問題の自動評価で、外部AIは1回ごとの明示確認が必要。既存のサーバースイッチとAPIキーだけでは自動送信しない。
- `PACHI_AI_<AI名>_INPUT_USD_PER_MTOK`、`OUTPUT_USD_PER_MTOK`、`MONTHLY_LIMIT_USD`（共通上限は`PACHI_AI_MONTHLY_LIMIT_USD`）を管理者が設定する。単価または月上限が未設定、予約額が上限超過、1要求40回超（`PACHI_AI_MAX_CALLS_PER_REQUEST`で変更可）の場合は外部通信前に停止する。
- 送信予定本文を保存せず検査し、メール、電話番号、カード番号、APIキー・パスワードらしい文字列があれば停止する。個人の実戦履歴、収支、会話履歴を外部へ送る設定は廃止した。
- 複数AI比較は、選択した全AIの個人情報・単価・上限を先に点検する。1社でも不備があれば他社も含め外部通信0回で停止する。
- `data/ai_usage.db`へ実行ID、AI名、モデル、用途、入力ハッシュ、予約上限、報告トークン、費用、失敗区分を追記保存する。質問本文、根拠本文、生回答、APIキーは保存しない。記録の更新・削除は禁止する。
- 同じ実行IDは再送しない。HTTP応答が不明な場合に同じIDで外部AIを二重に呼ばない。固定評価は既存の保存結果を返す。
- PC・スマホのAI接続欄に月上限と予約済み額、本文非保存、毎回確認、重複防止を表示する。通常質問は初期値でローカルの統計説明を使う。

## 安全上の境界

費用はAI事業者の請求額ではなく、管理者が設定した単価と事業者が返した使用量による概算。送信前はUTF-8バイト数と最大出力から保守的な金額を予約する。事業者が使用量を返さない場合は実額を記録できないため、予約額を月上限から引いたままにする。

個人情報検出は誤送信を減らすための形式検査であり、完全な匿名化を保証しない。そのため実戦履歴・会話履歴は入力経路自体から除外し、外部AI利用時も公開統計・固定質問・その場で入力した質問文だけに限定する。

外部AIの応答は引き続き根拠ID契約と共通チェックを通し、予測順位・着席判定・勝率を変更しない。3.49の採用審査前に比較結果から既定AIを自動選択しない。

## 確認結果

- 外部通信を使わない自動テストで、個人情報検出、料金未設定、月上限超過、追記専用履歴、本文・鍵の非保存、重複ID拒否、複数社の事前一括停止を確認。
- 実AIへの通信・課金、実ブラウザ・実iPhone・EXEでの手動確認、GitHub push、公開PWA/API配信、EXE再ビルドは未実施。

Python全体 **622件成功**、JavaScript **15テストスクリプト成功**。PC・スマホの主要スクリプト構文確認、差分の空白エラー検査も成功。既知のStarlette TestClientのhttpx移行警告1件。
