# v3.24 — 予測方式の共通比較・検証環境

実装開始：2026-09-08。最終確認：2026-09-09。
ローカル実装。公開PWA/APIへの配信・Windows EXE再ビルドは未実施。

## 範囲

改訂した予測特化工程の3.24。「新モデルを採用する前に、単純な方法より役立つかを同一条件で測る」段階。
3.25以降の台番号・入替履歴整備、予測モデルの自動採用、AI学習は実施していない。

- 四條畷周辺のスマスロ：機種平均と台番号を別々に比較。
- 現在の本体予測、単純平均、直近14暦日の平均、同曜日平均の4方式。
- 店舗全体の順位、ジャグラー、高設定的中率、本人勝率は今回の比較対象外。
- 着席基準を緩めず、比較結果による本体モデルの切り替えもしない。

## 固定条件（paired-baselines-v1）

- 前日までに確定した入力から、同じ店舗・同じ機種・同じ台番号を評価。
- 本体予測は既存の検索処理全体を利用し、比較用に別の近似モデルを作らない。
- 基準モデルは入力期限以前の監査済み実績を使う。差枚欠損・矛盾は除外、重複は1日1対象に統合。
- 対象別に実績14日以上が必要。直近平均は入力期限を含む14暦日。
- 同曜日が3日未満なら単純平均にフォールバックし、保存記録に明示。
- 3基準モデルの差枚予測は算術平均、プラス確率は `(プラス日数+2)/(実績日数+4)`。
- 4方式がすべて計算できる共通対象だけで誤差を比較。除外件数と理由を別表示。
- 実績は同日・同店・同機種・同台番号に完全一致させる。機種入替、欠損、矛盾は結果待ち。差枚0は非プラス。
- 共通の参考シグナルは確率70%以上かつ予測差枚0枚超。実際の着席推奨とは別。方式ごとの対象件数も必ず表示。

## 指標

- Brier：確率とプラス／非プラスのズレ。小さい方が良好。
- 日別Brier：各日のBrierを同じ重さで平均。同日に多くの機種がある影響を抑える。
- 平均絶対誤差：差枚予測の外れ幅。小さい方が良好。
- 符号一致率、参考シグナルの候補率・照合件数・プラス率。
- 現在の予測と各基準モデルの日別Brier差、改善／悪化／同等の日数。
- モデル版×機種平均／台番号を混ぜず、店舗別・機種別の内訳も表示。
- 未照合は誤差0・的中0%に置き換えない。日単位でも相関が残るため有意差・精度保証は自動認定しない。

## 本番の事前比較

`save_batch` がサーバー内で基準モデルも計算し、既存の改変不可ペイロードに同時保存する。
同日分の再実行で本体・基準モデルとも上書きしない。以前のバッチに後付けもしない。
v3.23で保存済みの2026-09-09分262件はそのまま保持し、比較値がない旧版として対象外を明示。
次回以降に新規保存する予測から、本番比較が始まる。過去検証レポートを本番成績へ移さない。

## 研究用の過去検証

- 最新の公開差枚日（四條畷周辺・スマスロ・直近180日から検索）までの7暦日を固定。好成績の日だけ選ばない。
- 各対象日の前日13:00の保存を想定し、入力は対象日の2日前まで。120暦日の範囲で検索を再実行。
- 検索の120日指定が両端込み121日になっていた範囲も120日に修正。
- `as_of_date` は内部呼出し専用。通常APIで情報時点を外部から指定できない。
- 未来の実績、重複対象、対象日／入力期限不一致は検証失敗として拒否する。
- 日別の入力・予測、比較条件、照合した実績と出典を別レポートに保存。
- 検証レポートはSQLite `prediction_benchmark_report` に追記。更新／削除禁止トリガーとSHA-256付き。
- 今後の入力アーカイブはgzip圧縮＋Base64で保存し、SHA-256は圧縮前のJSONに対する値。画面には小さな概要のみ返し、進行度更新で毎回全アーカイブを解析しない。旧形式も読み取り可能。
- 実行中は日数・割合を表示。保存完了まで100%にはしない。エラー時は成功と表示せず、以前の完了レポートを保持する。
- 進行状態と重複実行防止は単一APIプロセス内。複数ワーカー対応は今後。再起動で未完了処理は終了し、再実行が必要。
- 当時の取得時刻・公開元の訂正前データ・当時の全設置台は完全復元できない。このため事前成績とは別の研究用評価。
- 同じ期間を何度も調整しても未知期間の証明にはならない。モデル採用判断は後工程。

## UIとAPI

狙い台捜索 → データから狙い台を探す →「予測方式を同じ条件で比較」。
「本番：前日に固定した方式の成績」と「研究用：過去7日を同じ条件で検証」を別々に開く。

- GET `/api/predictions/comparison`：閲覧のみ。本番比較・最新過去レポート・進行状況。
- POST `/api/predictions/benchmark`：固定7日検証をバックグラウンド開始。重複は409。日付や予測値の任意指定は受け取らない。
- モバイル／ブラウザ／Windowsソフト共通コンポーネント、更新履歴、キャッシュ版を3.24.0へ更新。
- 既存のカード用スタイルが検証欄の内部にも適用され、横一列になっていた崩れを修正。
- 表はスマホでは横スクロールでき、画面全体を横にはみ出させない。

## 検証記録

- Python全体281件成功。依存ライブラリの非推奨警告1件（失敗ではない）。
- JS構文・PC／スマホUI契約テスト・比較欄のHTMLエスケープ検証に成功。
- 390px幅で、表を開いた状態でも画面全体が横にはみ出さず、表内のみ横スクロールできることを実画面で確認。
- PC共通画面でも同一の件数・成績を確認。ブラウザのエラー／警告なし。実画面の開始ボタン→進行度→完了レポートを確認。
- 完了済みレポートの再読込は概要キャッシュを使用。実データでは初回約0.74秒、2回目は計測上0.001秒未満。新しいレポートのハッシュが変われば再取得する。

## 最終の実データ比較（2026-09-09）

レポートID 2、対象2026-08-25～08-31、前日13:00想定、120暦日。
SHA-256：`adfda6222cb731c7c8076de7cec646c5e0d9ec1796d5ab77c5871d1129b51f20`。

| 対象 | 比較可能 | 照合済み | 結果の日数 | 現在のBrier | 単純平均のBrier |
|---|---:|---:|---:|---:|---:|
| 機種平均 | 946 | 222 | 7 | 0.2365 | 0.2325 |
| 台番号 | 254 | 24 | 2 | 0.2293 | 0.2116 |

機種平均は724件、台番号は230件が未照合。公開欠測・機種変更などを負け扱いにしていない。
今回の確率誤差は単純平均の方が良好。ただし期間が短く、特に台番号は2日しか結果がないので方式の優劣を確定しない。
機種平均の70%参考シグナルは4/5件プラスだが、5件だけで80%精度と主張しない。台番号の同条件は照合0件。

開発途中のID 1も監査履歴として保持しているが、最終結果は情報締切を揃えたID 2。
両レポートは圧縮実装前に保存済みなので書き換えていない。今後の同規模データは圧縮前約64.8MBからgzip約3.0MB（Base64と概要を含めて約4MB）に縮小可能。
旧v3.23事前バッチは1日分262件のままで、更新・後付けなし。

## 配信状態

GitHubへ未push、公開PWA・Fly APIは未更新、EXEは未再ビルド。
更新対象ファイルとデスクトップの版情報は揃えたが、既に配布したソフト自体は変わっていない。
ローカルDBの予測台帳・比較レポートはGit管理外。別PC／本番へ移すときはDBバックアップが別途必要。
