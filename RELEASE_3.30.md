# v3.30.0：予測モデルの比較・採用審査

2026-09-13。ローカル実装。詳細はこのファイルへ集約し、全ソース集は再生成しない。

## 実装

- 3.26イベント・3.27機種・3.28台番号・3.29並び／島を共通の審査画面へ整理。
- 過去の研究試行は既存結果を再利用し、事前保存成績とは分離。検索の再実行は不要。
- 新しい前日予測に、候補・基準・現行方式の同一対象予測、対象台・観測期間、審査条件、実装ハッシュを同時保存。
- 事前予測だけから「保留／却下／採用可・未適用」を判定し、理由・対象数・照合日数・誤差・改善幅を表示。
- 審査レポートは根拠とともに追記保存。同じ根拠なら同じレポートを返し、新しい結果が増えたら旧レポートを残して追加。
- 現行予測・確率・着席判定は変更しない。採用可でも実戦への切替は別作業。確率校正、適用範囲、切替・復帰の確認が必要。

入口：ホーム → 狙い台捜索 → 検索 → **予測モデルの採用審査（3.30）**。
「事前成績を審査して保存」は保存済み予測と答え合わせ結果だけを読み、重い過去検索や収集を開始しない。

## 固定した審査基準

| 項目 | 条件 |
|---|---|
| 対象 | 四條畷周辺、同じ実装ハッシュと審査条件の全保存対象。好成績の店・機種だけ選ばない |
| 期間 | 直前の完了した日曜日まで84暦日 |
| 必要量 | 30照合日・8週以上、全保存対象の照合率80%以上、期間末尾14日内に照合実績 |
| 比較 | 候補・基準・現行方式を同じ対象と日付で比較。現行値がない対象は保留 |
| 誤差 | 1台当たり差枚の絶対誤差。対象→店舗日→日→週の順に平均し、台数の多い日へ偏らせない |
| 改善 | 基準・現行の両方より50枚以上かつ5%以上改善、前半・後半とも改善 |
| ばらつき | 固定シードの週単位再標本化2000回、下側0.05/(4方式×2比較)分位点が0超 |
| 欠損 | 結果待ち・矛盾・低稼働・入替を0や負けにしない。列の全台が揃わなければ未照合 |
| 保全 | 実装・入力期限・保存時点・ハッシュを照合。不整合なら保留。旧予測・審査を上書きしない |

上記は運用上の審査ルールであり、統計的な優位性の厳密保証ではない。週をまたぐ相関、繰り返し審査、公開データの偏りも残る。

「基準方式」は機種／台／組では研究モデルと同期間の平均、イベントでは既存の同曜日比較基準。店舗イベントには同じ対象定義の現行予測がないため事前の三方式比較は保留。この不足を機種予測の部分集合平均で埋めない。
台番号の審査指標は差枚誤差であり、上位順位の的中率や本人の勝率の保証ではない。

## ファイル・保存・API

- `hall/model_selection.py`：研究試行の集約、比較値の固定、事前審査、追記保存。
- `hall/prediction_log.py`：前日ペイロードへ `model_selection` を追加。既存ペイロードは変更なし。
- `api/routers/hall.py`：検索結果へ軽い `model_review_research` 集計を付加。
- `api/routers/predictions.py`：`GET /api/predictions/model_review` は保存結果の読取のみ、`POST` は固定条件で審査・保存。
- `prediction_model_review`：時刻・条件・期間・実装ハッシュ・根拠ハッシュ・レポート。更新／削除禁止トリガー、根拠単位の重複防止。
- `mobile/model-review.mjs`：PC・PWA共通の表示・操作。通信失敗と二重送信を処理し、自動ポーリングなし。

## 確認と現在の制約

- 全体Python **377件成功**（40.38秒）。関連テストを先に実行し、最後に全体を1回実行。
- JS5テストファイル成功、PC・PWAのJS構文チェック成功。
- 機種名正規化、4系列の固定、欠損と0、未来・旧版の排除、入替、日別重み、期間不足、弱い改善、前後半不一致、鮮度、追記保存・冪等性、読取API、排他、XSSを検証。
- 確認用サーバーは元DBのコピーを利用し、自動収集・定期予測は停止。元DBへテスト成績は追加しない。
- スマホ幅390px・PC幅1280pxで表示を確認。スマホで審査を保存し、PCでも同じ審査#1を読めた。保存結果更新も成功、両画面のコンソール警告・エラーはなし。
- 9月13日の実データコピーでは、固定期間6月15日〜9月6日の新プロトコル対象が0件で4系列とも保留。旧版の固定予測がないという意味ではなく、新審査条件との同時保存がないため対象外。
- 過去研究の機種比較は7,477試行・97日、店舗日を等重みとした基準誤差1,005.6枚→候補994.7枚。他3系列の研究試行は0。この参考差を事前成績・現行方式に対する優位性・的中率とは扱わない。
- 新プロトコルの事前成績が不足しているため実戦への新モデル適用はなし。過去の好成績を新プロトコルの事前成績へ移し替えない。
- 既存テスト依存に非推奨警告1件。別途更新対象で、テスト失敗ではない。
- GitHub push、公開API/PWA配信、EXE再ビルドは今回未実施。ソース・表示版・更新履歴・キャッシュ版のみ3.30.0へ更新。

次は3.31：確率補正・予測幅の検証。採用可のモデルが将来出ても、実戦適用と復帰手順が未整備のまま自動切替しない。
