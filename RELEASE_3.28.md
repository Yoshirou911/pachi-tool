# v3.28.0 同機種内の台番号ランキング

完了日：2026-09-10。改訂予測特化工程の3.28のみ。3.29には進んでいない。

## 実装

- 狙い台捜索の共通検証欄の先頭に「台番号ランキング（3.28・研究用）」を追加。店舗→機種→番号を開き、研究順位・予測差枚・比較日数・不足理由・答え合わせ・出典を確認できる。
- 順位が付いた台と順位を付けられない台を分離。機種平均だけから番号を作らない。スマホでは台番号を大きく表示する。
- 3.25の観測期間分離を使用。機種変更、確認済みの配置変更、30日超の空白、矛盾、配置期限切れを扱い、同じ機種が戻ってきても旧期間を混ぜない。スマスロ以外への変更や差枚欠損の観測も期間分離に残す。
- 全対象スマスロに共通規則。対象日の2日前まで、現在の観測期間・前56日内の差枚と1000G以上の稼働が確認できる台だけを使う。同日の同機種の他2台以上と比較できた日が10日以上必要。最終比較日が入力期限から14日超古い場合も現在の研究用予測を出さない。
- 過去の同曜日の他台平均（3日未満なら同期間平均）に、対象台との差を `n/(n+20)` で縮小し±500枚に制限した補正を加える。対象日の結果は入力しない。これは固定の研究用方式で、実データを見て閾値を調整していない。
- 同じ店・同じ機種内でのみ予測差枚順に順位付け。比較可能な予測が1台だけなら順位なし。同点は同順位で、番号の大小は表示順にしか使わない。
- 期限内の新しい設置機種一覧がある場合は照合し、一覧にない機種の現在予測を停止。機種の設置情報は番号の現存保証ではなく、来店時の配置確認は必要。
- 過去最大60実績日を対象に、その時点の観測期間・配置情報・候補集合を再構築。現在の配置修正や現在残っている機種だけで過去を評価しない。
- 新順位と「同じ学習日の台ごとの単純平均順位」を、同じ対象台集合で比較。当時の1位を結果欠損後に差し替えない。対象全台の結果と期間が一致し、1000G以上の稼働が確認できる日に限り、1位と比較対象内の最良台との差を集計。同点1位は全該当台の実績を平均する。
- 台ごとの現在期間の差枚誤差も、両方式が予測した同じ日で比較。20日未満は検証不足。20日以上でも本番採用・統計的優位の証明ではない。
- APIと前日固定ペイロードに、順位・全試算・期間分離用の元観測・各時点で確認できる配置情報を同梱。画面の答え合わせは直近5件、出典は最大5件に絞る。旧固定予測・元実績を上書きせず、DB構造も変更しない。
- 新順位は研究用。本番採用審査は3.30で行う。従来の店舗・機種・台番号の判定と参考番号は変更しない。
- 版番号・更新履歴・キャッシュ版を3.28.0に統一。

## 検証

- Python全330テスト成功。既存のStarlette/httpx非推奨警告1件。
- 共通検証UI・スマホUI・PC UIのJavaScript契約テスト、構文チェック成功。
- 検証対象：自身を除く同機種比較、別機種・別店舗・未来結果の遮断、同点処理、欠損や低稼働、重複・矛盾、入替前後、答え合わせ当日の入替、配置情報の時点別分離、設置確認の期限、過去順位の保持、現在期間だけの誤差集計、固定ペイロードの不変性。
- スマホ幅390pxで実データの台番号・不足理由を確認。架空と明示した一時的な表示テストで、順位ありのカードも確認。PC版も共通パネルの表示・開閉を確認し、両画面のブラウザエラーログなし。テストデータは実績DBへ入れず、一時テストページは確認後に削除。
- 自動収集・自動固定を動かさない検証サーバーを使用。新しい本番予測の固定操作や収集操作は行っていない。

## 実データでの結果

条件：四條畷駅周辺、対象日2026-09-11、分析期間120日。新順位の入力期限2026-09-09。

| 店舗 | 台番号の観測対象 | 新方式の研究順位あり |
|---|---:|---:|
| ひま・わり四條畷店 | 0 | 0 |
| キコーナ四條畷店 | 0 | 0 |
| キコーナ大東店 | 0 | 0 |
| キコーナ野崎店 | 0 | 0 |
| スーパーコスモプレミアム大東店 | 17 | 0 |
| ニコニコ住道店 | 18 | 0 |
| ベガスベガス大東店 | 40 | 0 |
| マルハン大東店 | 7 | 0 |
| 合計 | 82 | 0 |

同機種の他2台以上と10日分比較する履歴が不足しており、実データの新順位・順位成績は未計測。観測82台は現在の全設置台数ではない。既存の参考番号や機種単位の成績を新方式の成績として流用していない。新方式による的中率向上はまだ確認できない。

検証環境で狙い台検索APIの完了は約40秒（全分析を含む1回測定）。環境・データ量で変動する。

## 限界と次工程

- 公開された台の偏り、未掲載日、低稼働による選別、結果公開時刻・改訂履歴の未整備が残る。全設置台を公平に網羅する分析ではない。
- 1位と最良台との差は事後の比較指標であり、高設定的中率や本人の勝率・利益ではない。比較用の台平均より優れたとしても、既存本番モデルを上回った意味ではない。
- 角・島・隣接・上げ据えの分析は3.29。番号の連続だけで隣接とみなさない。
- 実データ不足を解消するには、単発の好成績台だけでなく同店・同機種の複数台を同じ日ごとに継続収集する必要がある。今回は新しい収集や収集先追加は行っていない。
- 次工程は3.29。3.30の採用審査前に実戦推奨を自動昇格させない。

## 配信

ローカル実装のみ。GitHub push、公開API／GitHub Pagesの配信、EXE再ビルドは未実施。公開スマホ版・配布済みEXEは今回の作業だけでは更新されない。
