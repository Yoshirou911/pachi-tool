# v3.42.0：店舗特化AI分析

実装日：2026-09-19。ローカル実装。次は3.43「複数AI比較」。

## 追加したもの

- `api/ai_hall_analysis.py`：店舗・対象日・期間・質問項目・機種を限定する根拠抽出。読み取り専用で公開DBを参照し、3.41の根拠ID照合・固定文へ渡す。
- `POST /api/ai/hall_ask`：店舗の要点、曜日、日付末尾、機種、登録イベント、台番号履歴、島・配置の質問。一般チャット／レポートで1店舗を選んだ場合と店舗カルテ説明も共通処理を使う。
- `mobile/hall-ai.mjs`：PC・スマホ共通の質問欄。「店舗×強い機種／傾向分析」画面の下に追加。上で選んだ店舗・行く日・期間で質問し、項目ボタン、機種名の限定、根拠展開を利用できる。
- 通信中に店舗・対象日・期間・機種・質問を変えた場合、古い回答を表示しない。表示時点で外部AIを勝手に呼ばない。明示有効化までの接続停止と個人履歴の非送信を維持。

## 比較方法と制約

- 同店・同日・同機種は公表機種平均を優先し、なければ公開台の平均で補完。機種と台データを二重計上しない。別名の重複値が矛盾した区分は除外する。
- 曜日・日付末尾は公開機種平均を機種均等、さらに日ごと均等に集計し、同店のその他の日と比較する。設置台数で加重した全台平均ではない。機種構成、稼働差、イベントの重なりは未補正。日付末尾をイベント認定しない。
- 機種は同じ日に公表された他機種の均等平均と比較する。共通比較日がなければ差を0に置き換えない。比較対象の出典も根拠に含める。
- 5日未満（比較があるときは相手側も5日未満）は少数日・参考止まり。これは記述統計の表示基準であり、信頼度・勝率・確定設定を保証する閾値ではない。条件を満たす区分は平均差枚の差を説明するが、着席順位へ適用しない。
- 台番号は既存の `build_seat_history` で入替・情報矛盾・30日超の観測空白を分離する。数値は同一観測期間の履歴。台番号が表示されても行く日の機種・配置・空席・着席推薦とは認定しない。
- 島は当時までに記録された確認済み配置のみ使用。未確認、自動生成、有効期限切れ、重複フロアは除外。最新が未確認なら旧配置を復活させない。全構成台の実績が同日に揃い、配置の適用開始と記録日以後の日だけ集計する。台番号から隣接や角台を推測しない。
- イベントは登録済みの既存イベント分析・固定判定を引き継ぐ。別店舗・対象日以後の実績を含む根拠を除外。公式イベントの新規発見や新しい判定はしない。
- 他店との公平な比較に必要な同一日・同一機種・同一公開範囲の共通データは未整備。質問された場合は根拠不足と表示し、店内比較を「優良店／回収店」の断定へ転用しない。
- 根拠は30件まで、本文は3.41と同じ最大8件。要点は曜日・末尾・機種を交互に採用し、一分類だけで埋めない。出典は根拠ごと10件までの抜粋で、未記録の取得時刻も隠さない。対象日が未来なら本日分までを参照できるが、途中経過の可能性を明示。
- 質問分類は固定語と入力項目による。複雑な自然文の意味理解は保証しない。機種系列が複数に当たる曜日・台番号・配置は正式な機種名の限定が必要。AIは根拠を選ぶだけで、自由な分析文を生成しない。

## 検証

- Python全体499件成功。新規店舗特化テスト23件で質問分類、店舗／日付／機種限定、共通日比較、少数日、イベント分離、台番号の入替、確認済み配置、HTTP入力、固定判定非干渉を確認。
- UIテスト全スクリプト成功。質問欄の初期表示では通信しないこと、条件変更時の古い回答抑止、クイック質問、HTML無害化、既存画面契約を確認。PC・スマホ・Service Workerの構文チェック成功。
- 旧モデル審査テスト2件に実行日時が混ざる問題を確認し、テストの時計のみ固定。本番の答え合わせ日時・判定は変更していない。UIテストの旧3.33固定キャッシュ期待値も、共通版情報を参照する形に整理。
- ベガスベガス大東店の実DBを読み取り専用で確認。対象2026-09-20／365日指定の根拠は30件（曜日7、日付末尾10、機種13）。件数上限を含む説明用抽出数で、的中率の測定ではない。
- Starlette TestClientのhttpx移行警告1件あり。実ブラウザでの手動操作、外部AI実通信は未試験。

## 配信状態

外部AIへの実通信・課金・キー登録はしていない。GitHub push、公開API／PWA配信、EXE再ビルドは未実施。ローカル起動を再読み込みすると3.42が使える。公開スマホ版に反映済みとはしない。

3.43の複数AI比較、独自学習、予測モデルの採用、勝率向上の計測には進んでいない。
