# v3.33.0 店舗傾向と予測の変化を点検

実装日：2026-09-14。ローカル実装。公開PWA・API・EXEへの配信は未実施。

## できること

狙い台捜索 → データから狙い台を探す → **店舗傾向・予測の変化を点検（3.33）**。
PCとスマホで共通の点検画面を使う。店を開き、方式ごとの「要確認」「データ不足・判定保留」「固定基準の警告なし」と比較表を見る。

- 予測誤差の拡大と、観測された公開実績の変化を分離。
- 前日までに固定保存した予測と後日の結果台帳を利用。今日の計算で過去予測を捏造しない。
- 同一の版・実装・店舗・対象種類（機種平均／台番号）・方式を分離。4方式（現行・単純平均・直近14日・曜日）は別々に点検。
- 参考・見送りを含む保存対象の点検で、3.32の上位K候補だけの成績ではない。
- 保存0日や欠損を「予測悪化」「回避成功」と扱わない。未計測と0を区別。
- 画面表示・再確認時の読取集計のみ。常時監視の通知、メール、定期ジョブ、モデル切替、着席判定への加点は追加しない。

## 先に固定した比較条件

| 項目 | 条件 |
|---|---|
| 期間 | 昨日まで直近28日と、その直前56日。重複なし |
| 共通対象 | 店舗・機種・台番号キーが両期間にある対象の共通集合。結果の良し悪しで選ばない |
| 集計日 | 共通対象全件の予測と有効結果が揃った日だけ。未保存対象・一部欠損のある日は除外して件数を明示 |
| 重み | 対象を平均→日を等重み。台数の多い日だけを重くしない |
| 日数 | 以前28日・6週以上、直近14日・3週以上、直近前半／後半14日それぞれ全件照合7日以上 |
| 照合率 | 共通対象の保存件数に対する有効結果80%以上、両期間で必要 |
| 対象構成 | 共通対象の保存件数が全保存件数の80%以上、両期間で必要。入替等で構成が大きく違う場合は保留 |
| 鮮度 | 昨日を基準として7日以内の全件照合 |
| 正解 | 公開差枚が0枚超。±0は非プラス。稼働不明・1000G未満は結果として使用しない |
| 確率誤差 | Brier誤差。以前より0.05以上の増加 |
| 差枚誤差 | MAE。以前より100枚以上、かつ以前の誤差の20%以上の増加 |
| 公開実績 | 平均差枚500枚以上、またはプラス率20ポイント以上の同方向変化 |
| 継続条件 | 直近の前半・後半の両方が、以前56日の基準から同じ条件を超える場合のみ警告 |

閾値は事後点検の暫定的な運用基準。統計的有意差の検定でも、実測済みの最適警告閾値でもない。継続条件を満たさない変化も比較表には表示する。
共通集合と完全観測日だけを使うことにも生存・公開選択の偏りがある。曜日・イベント構成、同機種でも入替や観測空白、系列相関を完全に調整していない。営業方針や設定配分の変更、回収店と断定しない。
版が変われば成績は別系列になるため、頻繁な更新直後は監視材料が不足する。旧版を自動合算して不足を解消しない。警告なしも安全・精度・利益を保証しない。

## 実装と依存関係

- `hall/prediction_monitor.py`：固定期間、共通対象、全件照合日、誤差・観測変化、継続条件、保留理由。
- `hall/probability_validation.py`の`read_samples`を再利用。保存時点・入力期限・ペイロード整合性・結果証拠・既知時刻・稼働条件を共通検査。
- `api/routers/predictions.py`：GET `/api/predictions/monitor`。クライアントの期間・対象地域・閾値の指定は不使用。POSTなし。再学習・検索・保存・答え合わせなし。
- `mobile/prediction-monitor.mjs` → `mobile/prediction-verification.mjs` → PC/PWA共通画面。多重タップ防止、通信エラー・再試行、画面離脱、文字列エスケープ、横スクロール表。
- DBの新テーブルや既存予測の書換えなし。検知結果の履歴保存も未追加（応答は現在時点の点検）。
- バージョン・更新履歴・PC/PWAの読込識別・EXE用版情報を3.33.0に更新。EXE自体は未ビルド。

## 検証と現在地

- 関連Python48件成功後、全体 **448件成功**（29.37秒、新規20件）。既存FastAPIテスト依存の非推奨警告1件。
- JavaScript **8テストファイル成功**、PC/PWAのJS構文検証成功。文言修正後は当該UIテストのみ再確認。
- 期間境界、±0と欠損、継続・一時変化、逆方向変化、予測誤差と観測変化の分離、共通対象・部分照合・未保存・構成変化、日数・鮮度、重複、将来行除外、版・実装・店・対象種類・方式の分離を検証。
- 既存DB台帳の読み取り専用・地域・結果既知時刻・低稼働・改変検出、空DBのAPI無変更、XSS、多重タップ・通信失敗・再試行・離脱を確認。
- 9月14日の実台帳：6月22日〜9月13日に旧比較条件1日分、検証可能な保存0日、監視区分0。**実データの精度低下・警告精度は未計測**。実績DB全体が空という意味ではない。
- スマホ390pxでページ全体の横はみ出しなし。比較表の表示枠287px・内容480pxで横スクロール可能。要確認／不足／警告なし・開閉表示を架空データで確認。ブラウザー警告／エラーなし。
- PC1280pxでも共通画面・3区分・展開した4列の比較表を確認。ブラウザー警告／エラーなし。後続のラベル整理（平均差枚／プラス率の明記）はUIテストで再検証。
- 架空成績は確認サーバーのメモリ上のAPI応答のみ。実台帳や公開環境へ追加していない。

### 確認環境の起動時に発生した既存処理の影響

最初の確認サーバーでは設定読込後に`DATA_DIR`を切り替えたため、既存の`opportunity.models`読込時初期化が元DBのルール26件の`updated_at`だけを更新した。直ちに停止し、起動前バックアップとの全テーブル内容比較で、店舗実績・収支に差分なし、ルールの差分は時刻のみと確認。トランザクション内でも他列の一致を確認し、26件の時刻だけ復元、全ルールがバックアップと一致することを確認した。
以後は全アプリモジュールの読込前にコピー先を設定し、APIの参照先も検査して隔離。既存の「内容に差がなくても起動時にルール時刻を更新する」仕様自体は変更していない。次回の確認も必ず起動前に保存先を分離する。

## 次の工程

**3.40：根拠付きAI分析**。DBから得た確定数値・対象日・出典・不足理由をまとめて説明し、AIによる数値捏造や判定変更を防ぐ。モデル選択、鍵の配置、費用・外部送信範囲は別途確認する。
GitHub push・公開配信・EXE再ビルド・`SOURCE_SNAPSHOT.txt`再生成は未実施。
