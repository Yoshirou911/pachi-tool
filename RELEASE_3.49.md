# v3.49.1：AI工程の採用審査

実装日：2026-09-21（前日からの継続作業）。3.40〜3.48のローカル機能を点検し、用途を限定して採否を整理する版。外部AIを契約・有料実測・自動採用する工程ではない。

## 3.49.1公開パッチ

3.49.0のGitHub Pages配信は成功したが、Fly.ioの事前テストは、GitHub ActionsのUTC日付と配置履歴のJST記録日が日付境界で一致せず3件失敗し、API配信を開始しなかった。3.49.1ではCI、Fly.io、Dockerのタイムゾーンを`Asia/Tokyo`へ統一し、版表示・PWAキャッシュ・Windows数値版を更新する。AIの採否、予測方式、保存済み実績は変更しない。

## 審査結果と基準

| 区分 | 対象 | 認定する範囲 |
|---|---|---|
| 採用 | 固定問題の評価、誤回答チェック、費用・送信内容管理 | 開発用の検査・管理機構。実店舗の予測精度ではない |
| 補助限定 | 根拠付き説明、店舗傾向、画像入力、専用資料検索 | 出典・条件の照合や、人による訂正を前提にした補助 |
| 保留 | AI接続基盤の実品質、複数AIの比較・選定 | 各社との通信と実回答品質・実請求が未測定 |
| 不採用 | AIによる予測順位・着席判定の書き換え | 独立した事前成績の根拠がないため、この版では使わない |

上の認定には、現在の関連ソース・資料・テストとローカル検証記録の一致が必要。記録なし、不正な集計、版違い、ファイル変更・欠落の場合は認定を保留する。外部AIは単価・キー・接続許可がそろっても品質審査済みとはしない。人によるローカル点検記録であり、第三者署名による認証ではない。

## 画面・API

- スマホ：設定 → AI接続 → AI機能の審査結果。
- PC：AI分析 → AI機能の審査結果。
- `GET /api/ai/review` は閲覧のみ。外部通信・料金予約・採用変更・DB作成を行わない。
- 採否、認定範囲、未確認事項、外部AI別の費用設定、停止・復帰方法を表示。取得失敗時は古い合格表示を残さない。
- 旧PC画面の高設定断定・実戦履歴分析・着席推薦を連想させる質問ボタンを、公開履歴の説明に合う表現へ修正。

## 点検で修正した問題

1. 料金の事前確認と保存が分離しており、同時要求や複数AIで部分予約が起き得た。SQLiteの排他取引で全予約を一括確定／取消しする。
2. 外部呼出し後の結果記録だけでは、通信不明時・別インスタンスでの重複送信を防ぎ切れなかった。送信前に実行権を追記し、内容ハッシュ・最大出力と照合する。未知の使用量の予約は解放せず、開始時の単価を保持する。
3. UIの連打、変更前の同意再利用、通信結果不明後の新IDでの再送を防止。通常質問・比較APIで有料実行にはクライアント生成IDを必須にする。
4. 質問だけでなく送信予定の構造化資料内の文字列も検査。生成済み識別子を個人情報と誤認しない範囲で、店舗名・出典等への混入も点検する。
5. 訂正済み文字とは別に元OCR候補を保存していた問題を修正。新しい画面は未確認候補を送らず、サーバーも旧クライアントの候補を破棄。既存DB行は削除・書換えず、APIで元候補を再表示しない。**過去DBの元候補は物理的には残る**ため、従来DBのプライバシー整理は別作業。
6. 最新版の確認日が未来の場合に、古い資料が復活し得た問題を修正。登録済み最新版の可否を判定してから引用する。期待値資料の必須条件が欠ける場合はエラーや推測ではなく引用見合わせ。
7. EXEの数値版情報が文字版情報より古い点を修正。ビルド定義への反映のみで、EXEの再配布は行っていない。

## 検証記録

`scripts/verify_ai_release.py` が本番とは別の一時データ領域でPython全体・UI各スクリプト・主要JS構文を一度ずつ検証する。継承APIキー・外部AI設定・テスト選択設定を除外し、成功時のみ `data/ai_review/verification_3.49.json` を生成する。必須AI検査が一部だけ、失敗・スキップあり、実行途中のソース変更の場合は記録しない。

実行件数と日時は生成記録を正本とする。通信・失敗・課金記録のテストは模擬クライアントを使い、実AIへ送信しない。

2026-09-21の全体検証：**Python 716件成功、UI 16テストスクリプト成功、PC／スマホ主要JSの構文確認成功**。失敗・スキップなし。既存のStarlette TestClientのhttpx移行警告1件。差分の空白エラー検査も成功。公開前点検で、EXEのスモークテストが通常の収集スケジューラを起動しないことと、明示した隔離用`DATA_DIR`へ旧版DBを移行しないことの回帰テストを追加した。関連ソースを変更した場合は、この記録を流用せず再検証する。

## 残す境界

- 料金は設定単価と使用量報告に基づくローカル管理。事業者側の隠れたトークンや別料金を含む請求上限は保証しない。単価・月枠は管理者の設定を使い、事業者側の上限も併用する。
- 個人情報検出は形式検査で、完全な匿名化ではない。外部送信は公開資料と確認した質問だけ。実戦履歴・会話履歴は送らない。
- 月枠はUTC月の要求受付時点で管理。未完了・不明要求も予約を保持する。再起動で有料処理を自動再開しない。
- 実iPhone・EXE・実ブラウザ操作、公開先の配信／復帰試験は未実施。EXEでは照合用ソースがそろわないため審査を保留する場合がある。記録だけ同梱して合格扱いにはしない。
- 3.49.0のPWA配信は成功。公開APIは事前テスト失敗により3.10.1のまま維持され、3.49.1で再配信する。Windows 3.49.0はローカル作成済みで、3.49.1を再ビルドする。
- 次は3.50「朝一の候補画面」。今回の範囲には含めない。
