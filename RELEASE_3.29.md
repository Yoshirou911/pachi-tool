# v3.29.0：並び・島・翌日の傾向分析

実装・検証日：2026-09-13。ローカル実装済み。3.30の採用審査は未着手。

## 追加機能と見る場所

- 確認済みマップの島名・列名・列内順による、登録島と連続3台組の研究分析。
- 同日・同店・同機種の組外の台と比べた差枚、全台プラス日数、研究予測、組平均との誤差比較。
- 前日マイナス／プラス／±0から翌日の差枚傾向。設定の上げ・据えとは断定しない。
- 両日の設定値が確定した本人記録の比較。設定値の上昇／低下／同値を別集計。同値でも据え置きの証拠とはしない。
- PC・PWA共通表示、列入力・保存、配置改訂履歴。新分析と入力時点別マップ・最小限の設定記録を前日固定ペイロードへ保存。

ホーム → 狙い台捜索 → 日付・地域を選んで検索 → 「並び・島・翌日の傾向（3.29・研究用）」→ 店舗を開く。

登録はスマホ「店内」／PC「店内マップ」→「店内マップを登録・修正」→「確認した並び」。1行に `台番号,島名,列名,列内順`。番号順ではなく実際の隣接順を登録する。未入力台の列情報は不明扱い。仮配置を確認済みと偽らない。

## 分析の基準

- 入力期限は `min(情報日−1日, 対象日−2日)`。マップは記録時点と有効日を確認する。後日の配置を過去へ遡及利用しない。
- 最新配置が未確認・自動生成・期限切れ・競合なら古い確認済み配置へ戻さない。座標や台番号だけでは隣接を作らず、列内の欠番・重複も除外。
- 同じ配置・観測期間、前56日、全台1000G以上、各台と同機種の組外比較台2台以上が必要。10比較日以上、直近14日以内の実績が揃えば研究試算。
- 試算は他台平均＋縮小補正。同曜日の比較が3日以上あればその他台平均を使用し、組と他台の平均差を `日数/(日数+20)` で縮小、補正は±500枚。比較基準は同じ組の過去平均。
- 最大60観測日で、各日の2日前に配置・対象集合を再構成。全台実績が揃わない日は未照合とし、他台への差し替えや0補完をしない。
- 同じ日集合の平均絶対誤差を比較。20照合日以上でも未採用。旧配置の試行結果も残す。
- 翌日傾向は連続する暦日・同機種／同観測期間・両日1000G以上のみ。設定値比較は両日の確定記録が必要で、作成・更新日も期限内。メモ・投資・回収は分析入力へ含めない。

## 実データ確認

元DBを読み取りコピーした隔離環境で、四條畷周辺・対象2026-09-14・120日履歴・新分析の入力期限2026-09-12を確認。

| 店舗 | 翌日差枚傾向の区分数 |
|---|---:|
| スーパーコスモプレミアム大東店 | 43 |
| ニコニコ住道店 | 16 |
| ベガスベガス大東店 | 46 |
| 残る対象5店舗 | 0 |

区分は「機種×前日の符号」。105区分は独立した検証件数でも105機種でもない。少数日の区分を含み、予測精度を証明しない。

9月13日に元DBの機種日別100,799行・台日別51,639行、マップ・座席配置・改訂はすべて0行と再確認。台日別の最終日は8月26日。確認済み配置がないため並び・島の試算は全店舗0組。設定確定記録の連日比較も0組。実績・配置を捏造していない。

## 検証

- 全体Python：352件成功（29.63秒）。その後、固定保存と過去マップ復元の2ケースを追加し、関連27件成功。合計354ケース成功済み。追加後の全体再実行はしていない。
- JavaScript：新分析・入力、共通検証画面、PC・PWAの4テストファイル成功。両画面JSの構文チェック成功。
- 未確認／未来／期限切れ配置、列重複・欠番、矛盾、入替・A→B→A、欠損、低稼働、ペア誤差、確定設定根拠、旧DB移行、保存復元、XSSを検証。
- 390pxスマホと1280px PCで実検索・店舗開閉・不足説明を確認。隔離DBだけに架空・未確認の配置を保存し、番号順と異なる列順が保持されることを確認。元DBには追加していない。
- 既存のFastAPIテスト依存に非推奨警告1件あり。テスト失敗ではない。

## 限界・配信状態

- 新分析の数値は研究用で、現行着席判定に加点しない。差枚プラス率は高設定率・本人勝率ではない。
- 重複する3台組・同店同日の記録には相関がある。公開時刻・後日改訂履歴の不完全さ、公開台の偏りも残る。
- 今日マップを登録しても過去に知っていたことにはしない。現在の地図だけで過去検証が成立するわけではない。
- GitHub push・公開API/PWA配信・EXE再ビルドは今回未実施。ソース・更新履歴・キャッシュ版は3.29.0へ更新。
- 次は3.30「研究モデルの比較・採用審査」。不足なら保留を維持する。
