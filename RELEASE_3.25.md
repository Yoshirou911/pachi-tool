# v3.25.0 台番号・配置・入替の観測履歴

実装日：2026-09-09。改訂予測特化工程の3.25のみ。3.26には進んでいない。

## 実装内容

- 店舗名・機種名を正規化した店舗×台番号の観測期間を作成。
- 機種変更、確認済み配置変更、30日超の空白、同日の情報矛盾、配置期限切れを境界として扱う。
- A→B→Aの再設置でも、以前のAの実績を現期間へ混ぜない。スマスロ以外への入替・差枚欠損の機種情報も境界検出に使う。
- 台番号予測、比較用の固定入力、店内ヒートマップに現期間フィルターを適用。機種全体の歴史まで削らず、元実績・既存固定予測・旧比較レポートは保持。
- マップ保存時に追記専用の修正履歴を保存。保存日より前の検証に後日の修正を入れない。
- 数字が連続するだけの隣接補正・数字の両端を角とする判定を停止。古いマップの機種名に別機種の成績を付けない。
- 情報矛盾の番号も自動配置上から消さず、未評価として表示。
- PC／Windows共通画面・スマホ画面の「前日固定の予測を検証」に、店舗→台番号→期間・出典の開閉式履歴を追加。
- 読取専用の `/api/layouts/seat_history` を追加。店舗・基準日・期間（最大1095日）・台番号で絞れる。基準日より後は使用しない。
- 全クライアントの版番号・更新履歴・キャッシュ版を3.25.0へ更新。

## 検証

- Python：295テスト成功。既存のStarlette/httpx非推奨警告1件。
- JavaScript：共通検証UI・スマホUI・PC UIの契約テスト成功。
- A→B→A、異機種／欠損行の境界、別店舗・別番号の分離、矛盾、長期空白、配置変更・期限切れ、修正アーカイブ保持、API読取専用性、ヒートマップ、共通予測入力を検証。
- スマホ幅390pxで実データの履歴・店舗・番号の開閉と出典表示を確認。
- PC共通画面でも履歴の開閉と3.25.0表示を確認。両画面でブラウザのエラーログなし。Windowsの既存EXE自体は未検証。
- 検証画面では2026-09-08までの410番号の履歴を表示し、期間判定で1804行を分離。これは的中率・高設定判定の件数ではない。
- 改訂版の本番成績はまだ未測定。既存3.24の成績を3.25の成績として再利用しない。

## 制約・次工程

- 実物の製造番号を追跡する機能ではなく、入替日は公開実績日／マップ適用日の間でしか分からない。観測間に起きた未公開の入替は検知不能。
- 30日の空白判定は保守的な固定ルールであり、最適化済みのパラメーターではない。
- 既存マップで修正前履歴が残っていない分は復元できない。旧マップの取得時刻も不明。履歴保存開始後は日単位の既知情報で制限。
- 同一店内で複数フロアに同番号がある場合、元実績にフロア識別がないため実物の特定はできない。同日配置の矛盾は保留するが、完全なフロア別識別は未対応。
- 履歴の記録数は欠損・重複監査前。実際の予測にはさらに品質監査・対象機種の制限が入る。
- 次は3.26「店舗×イベント予測」。的中率・利益・高設定の保証ではない。

## 配信状態

ローカルソースへの実装のみ。GitHubへのpush、公開API／GitHub Pagesの配信、EXE再ビルドは未実施。公開スマホ版や既存EXEが3.25へ更新されたことを意味しない。
