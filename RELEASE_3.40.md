# v3.40.0：AI接続基盤

実装日：2026-09-14。AI専用工程3.40のみ。根拠データ契約を作る3.41には進んでいない。

## 実装内容

- `api/ai_provider.py`にGroq・DeepSeek・Qwen・KimiのOpenAI互換APIと、Claude Messages APIを共通化した接続層を追加。
- 接続先・モデル・基底URL・タイムアウトはサーバー環境変数で選択。APIキーは接続先ごとの環境変数だけから読み、レスポンス・DB・ブラウザ保存へ出さない。
- 旧Groq既定モデル`llama-3.3-70b-versatile`を、公式の移行候補`qwen/qwen3.6-27b`へ変更。
- `PACHI_AI_SEND_PERSONAL_DATA`は既定で無効。個人の実戦履歴を通常の店舗・機種統計と分離して外部送信しない。
- 認証失敗、レート制限、通信失敗、タイムアウト、空回答、形式不正を共通処理。既存の統計説明へ戻し、AIに予測・順位・着席判定を変更させない。
- `/api/ai/status`から接続先・モデル・利用可否・個人履歴送信可否・不足設定を返す。秘密の値そのものは返さない。
- PCのAI画面とスマホ設定に接続状態を表示。ハードコードされていたLlama/Groq表記を廃止。
- 不要になったGroq専用SDK依存を外し、既存依存の`httpx`へ統一。

## 設定

接続先は`PACHI_AI_PROVIDER`で`groq`、`deepseek`、`qwen`、`kimi`、`claude`のいずれかを指定する。必要なキー、初期モデル、任意設定は`README.md`を正本とする。

プロバイダーを自動巡回して別会社へ送信する機能は入れていない。設定した1社だけへ送信する。複数AIの同条件比較は3.43。

## 検証

- 接続設定、秘密非表示、外部送信停止、OpenAI互換形式、Claude形式、認証エラー、状態API、統計フォールバックを自動テストへ追加。
- 既存イベントAIの固定判定・台番号捏造防止テストを維持。
- PC・スマホJavaScriptの構文確認を実施。
- 全体Pythonテスト456件、モバイル主要テスト2本、JS8ファイルの構文確認に成功。Starlette TestClientのhttpx移行警告1件は既知事項として残す。

## 境界と次工程

- 外部サービスへの実通信と課金は行っていない。APIキーの契約・登録・公開サーバー設定も未実施。
- 3.40は通信基盤であり、AIの回答精度、台番号予測、的中率を向上させる工程ではない。
- 次は3.41「根拠付きAI回答」。対象日・店舗・機種・台番号・数値・出典・不足情報の受渡し形式を固定する。
- GitHub push、公開API／PWA配信、EXE再ビルドは未実施。
