# v3.22 / v3.23 予測特化チェックポイント

実装日：2026-09-08。ローカル実装。公開サイトの配信・Windows EXE再ビルドとは別。

## v3.22 — 入力品質

- スマスロ狙い台検索の機種・台番号実績を非破壊で監査。
- 欠損をゼロ化しない。不正な日付、非有限数、負のG数、不正台数を除外。
- 店舗名・機種名を正規化して重複排除。同日同機種／同日同台番号で数値や機種が矛盾する組は丸ごと保留。
- 日数、公開日の偏り、G数入力率、鮮度を表示。14日未満／公開日率40%未満／G数確認60%未満／14日超古い実績は参考限定。
- 公開日率の分母は初回観測日～入力期限の暦日。休業日や非公開日を判別できる指標ではない。
- 実績入力期限は「指定日の前日」と「日本時間で昨日」の早い方。対象時点より後の設置・座席情報も除外。
- 品質通過は勝率・高設定・店舗全台網羅を意味しない。

## v3.23 — 事前保存・答え合わせ

- 対象：四條畷周辺のスマスロの機種平均・台番号。店舗全体／ジャグラー／設定確定的中率はこの台帳には混ぜない。
- 固定方針：翌日分、120日、70%基準。表示上位に限らず算出済みの見送り・参考候補も保存。
- `prediction_batch` に入力実績・イベント根拠・設置・配置・モデル版・開始／保存時刻・入力期限・SHA-256を保存。
- `prediction_subject` に対象別の予測値・確率・判定・品質を保存。DBトリガーで更新・削除を禁止。日付×地域×方針で最初の1回だけ保存。
- `prediction_outcome` は別表。後日公開された同日・同店・同機種・同台番号を照合。未来／当日は照合しない。
- 結果は差枚0枚超をプラスとする。欠損・矛盾・機種不一致は結果待ち。0枚は欠損とは区別して非プラス。
- 最初の有効な公開結果と根拠を固定保存。後日の公開元訂正の履歴管理は今後の拡張。
- 自動実行はサーバー稼働中の毎日13:00 JSTと起動45秒後。停止日の過去予測は後付けしない。
- GET `/api/predictions/verification` は閲覧のみ。POST `/api/predictions/run` はサーバーが翌日分を計算して保存・照合する。任意のクライアント予測は受け取らない。
- 推奨の的中率は推奨の照合済み件数が分母。全保存・結果待ち・実日数・95% Wilson下限・全照合のBrierを別項目で返す。同日相関があるため自動で高信頼へ昇格させない。

## 見方

狙い台捜索 → 「データから狙い台を探す」 → 「前日固定の予測を検証」。
PC・Windows共通画面／スマホ版で同じ検証コンポーネントを使用する。
過去日を検索しても、この検証欄は保存済みの本番台帳の成績であり、過去検証に置き換わらない。

## 初回の実データ確認

- 2026-09-09分：機種186件、台番号76件、合計262件を保存。
- 推奨0件。すべて参考・見送り。実際の的中率はまだ未計測。
- 検索120日入力：機種12,781件から11,260件利用（重複1・除外1,520）、台番号5,015件から4,708件利用（除外307）。
- 元の収集実績を削除・書換えせず、追加の検証表は既存のhall_reports.db内に保存。

## 今回の境界と次段階

- v3.24以降のモデル改良・AI学習・新規データ収集は未着手。
- これは精度を正しく測る基盤。的中率70～80%や利益達成を保証する更新ではない。
- 公開配信するにはフロントとAPIの両方の更新が必要。GitHubへのpushだけでは稼働サーバーや既存EXEが必ず更新されるわけではない。
- ローカル台帳はGit管理外のDB。別PC／本番サーバーへ成績を移す場合はDBバックアップの移行が別途必要。

## 検証記録

- Python全体テスト259件成功（依存ライブラリの非推奨警告1件、テスト失敗なし）。
- モバイル／PC UI契約テスト、新しい検証コンポーネントの表示・エスケープ確認に成功。
- スマホ幅390pxで実データの品質・保存件数を表示。検証本文は14pxへ拡大。
- 実画面の手動再実行で「保存済みの予測を保持」を確認（初回262件は上書きなし）。
